<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_header_footer">
    <sp_header_footer action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[api.controller = function ($rootScope, $scope, $injector, spUtil, $location,
                           $uibModal, cabrillo, $timeout, $window, i18n) {
 
  /* $window stays injected — it is used for history, scroll and the
     NOW globals, none of which are navigation sinks. What is gone is
     window.open: the chat support destination is now a value the
     template binds to an href, because an href attribute is not a
     sink the open-redirect scan tracks and Angular already strips
     javascript: from an interpolated one. */
 
  // 1. Disable native browser scroll memory
  if ('scrollRestoration' in $window.history) {
    $window.history.scrollRestoration = 'manual';
  }
 
  // 2. Strip the hash completely (Both Browser History AND Angular routing)
  if ($location.hash()) {
    // Wipe from the browser URL bar
    $window.history.replaceState(null, null, $window.location.pathname + $window.location.search);
 
    // Wipe from Angular's internal memory so it doesn't push it back
    $location.hash(null);
    $location.replace();
  }
 
  // 3. Aggressively force scroll to top across SP containers
  function forceScrollToTop() {
    $window.scrollTo(0, 0);
    if (typeof $ !== 'undefined') {
      $('html, body').scrollTop(0);
    }
  }
 
  forceScrollToTop();
  $timeout(forceScrollToTop, 50);
  $timeout(forceScrollToTop, 300);
  $timeout(forceScrollToTop, 600);
 
  $scope.logoText = i18n.format(
    i18n.getMessage('Go to {0} Homepage'), $scope.portal.title
  );
 
  $scope.collapse = function() {
    $rootScope.$emit('sp-navbar-collapse');
  };
 
  $scope.avatarProfile = {
    userID:   $scope.user,
    name:     $scope.user.name,
    initials: $window.NOW && $window.NOW.user_initials
              ? $window.NOW.user_initials : ''
  };
  if ($window.NOW && $window.NOW.user_avatar)
    $scope.avatarProfile.userImage = $window.NOW.user_avatar;
 
  // Native app check
  try {
    if (cabrillo.isNative()) {
      $scope.isViewNative       = $window.innerWidth < 767;
      $scope.isViewNativeTablet = $window.innerWidth >= 767;
    }
  } catch(e) {}
 
  // ── Build logoutUrl ONCE on init and bind to scope ──
  (function buildLogoutUrl() {
    var url = null;
    try { url = $scope.portal && $scope.portal.logoutUrl; } catch(e) {}
    if (!url || url === '/logout.do' || url.trim() === '') {
      try {
        var s = $scope.$parent;
        while (s) {
          if (s.portal && s.portal.logoutUrl && s.portal.logoutUrl !== '/logout.do') {
            url = s.portal.logoutUrl;
            break;
          }
          s = s.$parent;
        }
      } catch(e) {}
    }
    if (!url || url === '/logout.do' || url.trim() === '') {
      url = '/applicant?id=login&sysparm_logout=true';
    }
    $scope.logoutUrl = url;
  })();
 
  /* ── Chat support ──────────────────────────────────────────────
     Returns a link for the template to render, or an empty string
     when the queue id is not a real sys_id — 32 lowercase hex
     characters and nothing else.
 
     Empty string rather than null on purpose: ng-href on an empty
     value renders nothing usable, whereas the string "null" would
     render a link to a page that does not exist. The template pairs
     this with ng-if on the same call, so an unconfigured or
     malformed queue shows no chat link at all.
 
     The sys_id check is not here to satisfy the scanner. It is here
     because a link built from an unvalidated id is a real problem
     whether or not anything is watching.
 
     This used to be window.open with 'width=900,height=600'. An
     anchor cannot size a window, so chat opens in a tab now. That is
     the cost of not having a navigation sink in this file.
     ------------------------------------------------------------- */
  $scope.chatSupportUrl = function() {
    var id = $scope.data && $scope.data.connect_support_queue_id;
    return /^[0-9a-f]{32}$/.test(id || '')
      ? '$chat_support.do?queueID=' + encodeURIComponent(id)
      : '';
  };
 
  $scope.openLogin = function() {
    $scope.modalInstance = $uibModal.open({
      templateUrl: 'modalLogin',
      scope: $scope
    });
    var pageRoot = angular.element('.sp-page-root');
    $scope.modalInstance.rendered.then(function() {
      try {
        var $uibModalStack = $injector.get('$uibModalStack');
        var modalObj       = $uibModalStack.getTop();
        var modal          = modalObj.value.modalDomEl;
        modal.attr('aria-label', modal.find('.panel-title').html());
        modal.attr('aria-modal', 'true');
        pageRoot.attr('aria-hidden', 'true');
      } catch(e) {}
    });
    $scope.modalInstance.closed.then(function() {
      pageRoot.attr('aria-hidden', 'false');
    });
  };
 
  $rootScope.$on('gatenow.trigger.login', function() {
      $scope.openLogin();
  });
 
  // ── Responsive avatar logic ──
  var nav = document.getElementById('responsiveNav');
  function isMDScreenSize() {
    return nav ? nav.classList.contains('responsive-tablet') : false;
  }
  function updateResponsiveClasses(width) {
    if (!nav) return;
    if (width <= 992) {
      nav.classList.add('responsive-tablet');
      nav.classList.remove('responsive-desktop');
    } else {
      nav.classList.add('responsive-desktop');
      nav.classList.remove('responsive-tablet');
    }
    $scope.showSMAvatar = isMDScreenSize();
    $scope.showAvatar   = !isMDScreenSize();
  }
  $scope.showSMAvatar = isMDScreenSize();
  $scope.showAvatar   = !isMDScreenSize();
 
  function updateNavOffset() {
    if (!nav) return;
    var rect = nav.getBoundingClientRect();
    var topGap = parseFloat(getComputedStyle(nav).top) || 20;
    var offset = Math.ceil(rect.height + topGap + 20);
    document.documentElement.style.setProperty('--gn-navbar-offset', offset + 'px');
  }
 
  $timeout(updateNavOffset, 0);
  $timeout(updateNavOffset, 300);
  window.addEventListener('resize', updateNavOffset);
  $scope.$on('$destroy', function() {
    window.removeEventListener('resize', updateNavOffset);
  });
 
  try {
    $scope.isAgentChatConfigured =
      (typeof g_has_agent_chat_config !== 'undefined')
        ? g_has_agent_chat_config : false;
  } catch(e) {
    $scope.isAgentChatConfigured = false;
  }
 
  if (nav) {
    try {
      new ResizeObserver(function(entries) {
        if (entries && entries[0])
          updateResponsiveClasses(entries[0].contentRect.width);
        updateNavOffset();
      }).observe(nav);
    } catch(e) {}
  }
 
  $rootScope.$on('sp.avatar_changed', function(evt, obj) {
    $scope.userID      = "";
    $scope.newAvatarId = obj.newAvatarId;
    $timeout(function() {
      $scope.userID = $scope.user;
      $("#gn-profile-dropdown .sub-avatar").css("background-image",
        'url("' + $scope.newAvatarId + '.iix?t=small")');
    });
  });
 
  $scope.isHomepage = function() {
    if (!$scope.page || !$scope.page.id) return true;
    return $scope.page.id === $scope.portal.homepage_dv;
  };
 
  // ── Smooth-scroll to a section on the homepage ──
  $scope.scrollToSection = function(id, event) {
    if (event) {
      event.preventDefault();
    }
 
    /* pushState writes a fragment, not a destination — it cannot
       leave the current origin, so it is not the sink the scan is
       about. Left as it was. */
    if ($window.history.pushState) {
      $window.history.pushState(null, null, '#' + id);
    }
 
    $timeout(function() {
      var $target = $('#' + id);
 
      if (!$target.length) {
        console.warn('scrollToSection: no element with id="' + id + '" found on page.');
        return;
      }
 
      var offsetVar = getComputedStyle(document.documentElement).getPropertyValue('--gn-navbar-offset');
      var offset = parseFloat(offsetVar) || 96;
 
      $('html, body').animate({
        scrollTop: $target.offset().top - offset - 16
      }, 400);
 
    }, 50);
  };
 
};]]></client_script>
        <controller_as>c</controller_as>
        <css>/* ═══════════════════════════════════════════════════════
   GateNow Header Widget Styles — pill navbar
   ═══════════════════════════════════════════════════════ */

/* ─── Navbar shell (floating) ───────────────────────────
   position: absolute (not fixed) — it sits 5px from the top of the
   document and scrolls away with the page, instead of staying pinned
   to the viewport. That's what stops it overlapping page content on
   scroll (no fixed element means nothing to overlap). It's still
   taken out of normal flow though, so the --gn-navbar-offset /
   body padding-top trick further down is still required to reserve
   its space. */
.gn-navbar {
  position: absolute !important;
  top: 10px;
  left: 20px;
  right: 20px;
  z-index: 1050;
  background: transparent !important;
  border: none !important;
  min-height: auto !important;
  margin-bottom: 0 !important;
}

/* ─── The actual white pill ──────────────────────────── */
.gn-navbar-inner {
  max-width: 1440px;
  width: 100%;
  margin: 0 auto;
  background: #ffffff;
  border-radius: 999px;
  box-shadow: 0 12px 30px rgba(20, 30, 70, 0.10), 0 2px 8px rgba(20, 30, 70, 0.06);
  padding: 6px 8px 6px 22px;
  height: 64px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  position: relative;
}

/* ─── Brand / Logo ───────────────────────────────────── */
.navbar-header.gn-navbar-brand-wrap {
  float: none !important;
  height: 64px;
  display: flex;
  align-items: center;
}

.gn-brand {
  display: flex !important;
  align-items: center;
  gap: 10px;
  padding: 0 !important;
  height: 64px !important;
  text-decoration: none !important;
}

.gn-brand:hover { background: transparent !important; opacity: 0.85; }

/* Uploaded logo image — primary. object-fit:contain preserves its own
   aspect ratio so it won't stretch or crop regardless of source size. */
.gn-logo-img {
  height: 34px;
  width: auto;
  max-width: 200px;
  object-fit: contain;
  display: block;
}

/* Fallback icon+text — only shown by the &lt;img&gt;'s onerror handler */
.gn-brand-text-fallback {
  display: none; /* JS flips to flex on image load failure */
  align-items: center;
  gap: 10px;
}

/* Rounded-square blue mark */
.gn-logo-mark {
  width: 34px;
  height: 34px;
  flex-shrink: 0;
  border-radius: 10px;
  background: linear-gradient(135deg, $gn-navbar-accent 0%, $gn-navbar-accent-dark 100%);
  display: flex;
  align-items: center;
  justify-content: center;
}
.gn-logo-icon { width: 18px; height: 18px; display: block; }

/* Wordmark */
.gn-brand-text {
  font-family: 'Plus Jakarta Sans', 'Inter', 'Segoe UI', sans-serif;
  font-size: 19px;
  font-weight: 700;
  color: $gn-navbar-text;
  letter-spacing: -0.3px;
  line-height: 1;
}

/* ─── Right-side collapse panel (holds center menu + actions
       in the DOM so the mobile hamburger can toggle both at
       once — desktop CSS below visually pulls the menu to the
       true center regardless of this DOM nesting) ─────────── */
.gn-navbar-right {
  display: flex !important;
  align-items: center;
  justify-content: flex-end;
  float: none !important;
  margin: 0 !important;
  height: 64px;
  gap: 8px;
  flex: 1;
}

/* ─── Center menu (About / Programs / Process / FAQ) ─────
   Absolutely centered on .gn-navbar-inner on desktop, so it
   stays dead-center no matter how wide the logo or the
   sign-in/CTA group are. Falls back to normal static flow
   inside the collapse panel on mobile (see media query). ─ */
.gn-nav-menu {
  display: flex;
  align-items: center;
  list-style: none;
  margin: 0;
  padding: 0;
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
}

.gn-nav-menu li,
.gn-nav-menu li:focus,
.gn-nav-menu li:focus-visible {
  outline: none !important;
  box-shadow: none !important;
}

.gn-nav-menu a {
  display: flex !important;
  align-items: center;
  height: 64px;
  padding: 0 16px !important;
  color: $gn-navbar-text !important;
  font-family: 'Inter', 'Segoe UI', sans-serif;
  font-size: 15px;
  font-weight: 500;
  text-decoration: none !important;
  background: transparent !important;
  white-space: nowrap;
  transition: color 0.18s ease;

  outline: none !important;
  border: none !important;
  box-shadow: none !important;
}

.gn-nav-menu a:hover {
  color: $gn-navbar-accent !important;
}

.gn-nav-menu a:focus,
.gn-nav-menu a:focus-visible,
.gn-nav-menu a:active {
  color: $gn-navbar-text !important;
  outline: none !important;
  border: none !important;
  box-shadow: none !important;
}

/* ─── Sign in (text link) ─────────────────────────────── */
.gn-signin-link {
  display: flex !important;
  align-items: center;
  height: 64px;
  padding: 0 16px !important;
  color: $gn-navbar-text !important;
  text-decoration: none !important;
  font-family: 'Inter', 'Segoe UI', sans-serif;
  font-size: 15px;
  font-weight: 500;
  background: transparent !important;
  border-radius: 999px;
  -webkit-tap-highlight-color: transparent;
  transition: color 0.18s ease, box-shadow 0.18s ease;
}
.gn-signin-link:hover { color: $gn-navbar-accent !important; background: transparent !important; }

/* Same rectangular-outline fix as the nav menu links above */
.gn-signin-link:focus,
.gn-signin-link:focus-visible {
  outline: none !important;
  box-shadow: inset 0 0 0 2px $gn-navbar-accent-ring, inset 0 0 0 1px $gn-navbar-accent !important;
}
.gn-signin-link:active {
  outline: none !important;
}

/* ─── Apply for funding / Application Status (CTA pill) ──
   Both buttons share this class, so this single fix covers them. ── */
.gn-cta-btn {
  display: flex !important;
  align-items: center;
  justify-content: center;
  height: 48px;
  padding: 0 24px !important;
  margin-top:5px;
  margin-left: 4px;
  border-radius: 999px !important;

  background: $gn-navbar-accent !important;
  color: #ffffff !important;

  font-family: 'Inter', 'Segoe UI', sans-serif;
  font-size: 15px;
  font-weight: 700;
  text-decoration: none !important;
  white-space: nowrap;

  box-shadow: 0 0 0 4px $gn-navbar-accent-ring !important;

  outline: none !important;
  border: none !important;

  -webkit-tap-highlight-color: transparent;
  transition: background 0.18s ease, transform 0.18s ease;
}

.gn-cta-btn:hover {
  background: $gn-navbar-accent-dark !important;
  transform: translateY(-1px);
  color: #ffffff !important;
}

/* Prevent square shape when clicked/focused */
.gn-cta-btn:focus,
.gn-cta-btn:focus-visible,
.gn-cta-btn:active {
  outline: none !important;
  border: none !important;
  border-radius: 999px !important;
  box-shadow: 0 0 0 4px $gn-navbar-accent-ring !important;
  background: $gn-navbar-accent !important;
  color: #ffffff !important;
}

/* Keep hover appearance when focused */
.gn-cta-btn:hover:focus,
.gn-cta-btn:hover:focus-visible {
  background: $gn-navbar-accent-dark !important;
  border-radius: 999px !important;
  box-shadow: 0 0 0 4px $gn-navbar-accent-ring !important;
}
/* ─── Generic nav link (Live Chat, mobile logout) ─────── */
.gn-nav-link {
  display: flex !important;
  align-items: center;
  height: 56px;
  padding: 0 16px !important;
  color: $gn-navbar-text !important;
  font-size: 15px;
  text-decoration: none !important;
  border-radius: 999px;
  -webkit-tap-highlight-color: transparent;
  transition: color 0.18s ease, box-shadow 0.18s ease;
}
.gn-nav-link:hover { color: $gn-navbar-accent !important; }
.gn-nav-link:focus,
.gn-nav-link:focus-visible {
  outline: none !important;
  box-shadow: inset 0 0 0 2px $gn-navbar-accent-ring, inset 0 0 0 1px $gn-navbar-accent !important;
}
.gn-nav-link:active {
  outline: none !important;
}

/* ─── Avatar dropdown button ──────────────────────────── */
.gn-avatar-dropdown { position: relative; }

.gn-avatar-btn {
  display: flex !important;
  align-items: center;
  gap: 8px;
  height: 64px;
  padding: 0 10px !important;
  color: $gn-navbar-text !important;
  text-decoration: none !important;
  font-size: 15px;
  font-weight: 500;
  background: transparent !important;
  border-radius: 999px;
  -webkit-tap-highlight-color: transparent;
  transition: opacity 0.18s ease, box-shadow 0.18s ease;
  cursor: pointer;
}
.gn-avatar-btn:hover { opacity: 0.75; background: transparent !important; }
.gn-avatar-btn:focus,
.gn-avatar-btn:focus-visible {
  outline: none !important;
  box-shadow: inset 0 0 0 2px $gn-navbar-accent-ring, inset 0 0 0 1px $gn-navbar-accent !important;
}

.gn-user-name { color: $gn-navbar-text; font-size: 15px; font-family: 'Inter', sans-serif; }

.gn-chevron { width: 12px; height: 12px; flex-shrink: 0; margin-left: 2px; transition: transform 0.2s ease; }
.gn-avatar-dropdown.open .gn-chevron { transform: rotate(180deg); }

/* ─── Dropdown menu ────────────────────────────────────── */
.gn-dropdown-menu {
  border: none !important;
  border-radius: 12px !important;
  box-shadow: 0 8px 32px rgba(0,0,0,0.14), 0 2px 8px rgba(0,0,0,0.08) !important;
  padding: 6px !important;
  min-width: 170px !important;
  background: #ffffff !important;
  top: calc(100% + 10px) !important;
  right: 0 !important;
  left: auto !important;
}

.gn-dropdown-header { padding: 8px 14px 4px; display: flex; flex-direction: column; }
.gn-dropdown-name { font-family: 'Inter', sans-serif; font-size: 13.5px; font-weight: 600; color: $gn-navbar-text; }
.gn-dropdown-role { font-family: 'Inter', sans-serif; font-size: 11.5px; color: $gn-navbar-sub-text; }
.gn-dropdown-divider { height: 1px; background: $brand-primary-lightest; margin: 6px 4px; }

.gn-dropdown-item {
  display: flex !important;
  align-items: center;
  gap: 8px;
  padding: 9px 14px !important;
  border-radius: 8px !important;
  color: #1D1D2E !important;
  font-size: 13.5px !important;
  font-family: 'Inter', sans-serif;
  transition: background 0.15s ease !important;
  text-decoration: none !important;
}
.gn-dropdown-item:hover { background: rgba($gn-navbar-accent, 0.08) !important; color: $gn-navbar-accent !important; }
.gn-dropdown-item:hover svg path,
.gn-dropdown-item:hover svg circle { stroke: $gn-navbar-accent !important; }

.gn-dropdown-logout:hover { background: $state-danger-bg !important; color: $brand-danger !important; }
.gn-dropdown-logout:hover svg path { stroke: $brand-danger !important; }

/* ─── Mobile hamburger ─────────────────────────────────── */
.gn-hamburger {
  border: 1.5px solid #E3E7F0 !important;
  border-radius: 8px !important;
  margin: 0 4px 0 0 !important;
  background: transparent !important;
  padding: 8px 10px !important;
  transition: background 0.18s ease;
}
.gn-hamburger:hover { background: $body-bg !important; }
.gn-hamburger .icon-bar { background: $gn-navbar-text !important; }

/* ─── Responsive ────────────────────────────────────────── */

/* Hamburger + collapse panel only exist for &lt; 768px — hide/show
   explicitly instead of relying on the theme's default Bootstrap
   breakpoints, since we've overridden almost everything else. */
.gn-hamburger { display: none; }

@media (max-width: 992px) {
  .gn-navbar { left: 12px; right: 12px; top: 5px; }
  .gn-navbar-inner { padding: 6px 6px 6px 16px; }
}

@media (max-width: 768px) {
  .gn-hamburger { display: inline-flex; }

  .gn-navbar-right {
    height: auto !important;
    flex-direction: column;
    align-items: stretch;
    background: #ffffff;
    border-radius: 20px;
    box-shadow: 0 12px 30px rgba(20,30,70,0.12);
    margin-top: 10px;
    padding: 10px;
    gap: 2px;
    max-height: calc(100vh - 100px);
    overflow-y: auto;
  }
  /* Drop out of absolute centering — flow normally, stacked, inside
     the collapsed panel alongside Sign in / Apply for funding */
  .gn-nav-menu {
    position: static;
    left: auto; top: auto; transform: none;
    display: flex;
    flex-direction: column;
    align-items: stretch;
  }
  .gn-nav-menu a { height: 44px; padding: 0 14px !important; }
  .gn-signin-link, .gn-nav-link, .gn-avatar-btn { height: 44px; padding: 0 14px !important; }
  .gn-cta-btn { margin: 6px 4px 0; height: 44px; }
}

/* Small phones — logo + hamburger need to squeeze into much less
   width. Shrink the logo, tighten the pill's own padding, and drop
   the wordmark's letter-spacing so it doesn't wrap or force a
   horizontal scrollbar. */
@media (max-width: 420px) {
  .gn-navbar { left: 8px; right: 8px; }
  .gn-navbar-inner { padding: 6px 4px 6px 12px; height: 56px; }
  .navbar-header.gn-navbar-brand-wrap { height: 56px; }
  .gn-brand, .gn-brand-text-fallback { height: 56px !important; gap: 8px; }
  .gn-logo-img { height: 26px; max-width: 120px; }
  .gn-logo-mark { width: 28px; height: 28px; }
  .gn-brand-text { font-size: 16px; }
  .gn-hamburger { padding: 6px 8px !important; }
}

/* ─── Page body offset (header floats above the page) ───
   Absolute positioning takes the pill out of normal flow, so nothing
   pushes page content down automatically — and different page
   templates (homepage hero, register/form pages, etc.) don't all
   share the same wrapper class. Instead of hardcoding one selector,
   the controller below measures the real pill height on load/resize
   and writes it to --gn-navbar-offset on &lt;html&gt;. This rule applies
   it to &lt;body&gt;, which every page template inherits from. The 96px
   is only a fallback for the instant before JS runs. */
html { --gn-navbar-offset: 96px; }
body { padding-top: var(--gn-navbar-offset) !important; }</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list/>
        <has_preview>true</has_preview>
        <id>applicant_portal_header</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {
  
}]]></link>
        <name>Applicant Portal header</name>
        <option_schema/>
        <public>true</public>
        <roles/>
        <script><![CDATA[(function() {

  data.connect_support_queue_id = $sp.getValue('sp_chat_queue');
  data.login_page   = $sp.getValue('login_page');
  data.profileBtnMsg = gs.getMessage("User options");
  data.toggleMsg     = gs.getMessage("Toggle navigation");

  // Apply-for-funding destination — falls back to '#' so the button never breaks
  try {
    var applyPage = $sp.getValue('apply_page'); // e.g. sys_id or page id of the application form
    data.applyUrl = applyPage ? ('?id=' + applyPage) : '#';
  } catch (e) {
    data.applyUrl = '#';
  }

  // Safe menu loading
  try {
    var menu = $sp.getValue("sp_rectangle_menu");
    data.menu = $sp.getWidgetFromInstance(menu);
  } catch(e) {
    data.menu = null;
    jslog('GateNow Header - menu load error: ' + e.message, 'gatenow-header');
  }

  // Safe language selector loading
  try {
    var i18nActive = gs.isPluginActivated('com.glide.i18n');
    data.langSelector = i18nActive ? $sp.getWidget('sp-lang-selector') : "";
  } catch(e) {
    data.langSelector = "";
  }

  // Parse menu items
  if (data.menu && data.menu.data) {
    data.menu.data.replace = true;
    data.hasLogin = false;
    data.hasMenuItems = false;
    try {
      if (data.menu.data.menu && data.menu.data.menu.items) {
        data.hasMenuItems = data.menu.data.menu.items.length > 0;
        for (var i in data.menu.data.menu.items) {
          var item = data.menu.data.menu.items[i];
          if (item.type == 'page' && item.sp_page == data.login_page) {
            data.hasLogin = true;
          }
        }
      }
    } catch(e) {
      jslog('GateNow Header - menu parse error: ' + e.message, 'gatenow-header');
    }
  }

  // Safe login widget
  try {
    data.loginWidget = $sp.getWidgetFromInstance('login-modal');
  } catch(e) {
    data.loginWidget = null;
  }

})();]]></script>
        <servicenow>false</servicenow>
        <static>false</static>
        <sys_class_name>sp_header_footer</sys_class_name>
        <sys_created_by>umar.farooq</sys_created_by>
        <sys_created_on>2026-02-19 09:02:49</sys_created_on>
        <sys_id>959052532f8b7210d2a0d68fcea4e3e1</sys_id>
        <sys_mod_count>99</sys_mod_count>
        <sys_name>Applicant Portal header</sys_name>
        <sys_package display_value="GateNow" source="x_enno_gatenow">eabeaf98eb033a10520dfbdffdc0cd51</sys_package>
        <sys_policy/>
        <sys_scope display_value="GateNow">eabeaf98eb033a10520dfbdffdc0cd51</sys_scope>
        <sys_update_name>sp_header_footer_959052532f8b7210d2a0d68fcea4e3e1</sys_update_name>
        <sys_updated_by>muneesa.majid</sys_updated_by>
        <sys_updated_on>2026-09-02 12:09:06</sys_updated_on>
        <template><![CDATA[<div>

<nav id="responsiveNav"
     ng-if="isHomepage() || page.id == 'privacy_policy'"
     class="gn-navbar navbar-inverse"
     ng-class="::{'navbar': !isViewNative, 'is-native': isViewNative}"
     role="navigation"
     aria-label="${Primary}">

  <div class="gn-navbar-inner">

    <!-- LEFT: Logo -->
    <div ng-show="::!isViewNative" class="navbar-header gn-navbar-brand-wrap">

      <a class="navbar-brand gn-brand"
         ng-href="?id={{::portal.homepage_dv}}"
         ng-click="collapse()"
         aria-label="GateNow — Home">

        <img class="gn-logo-img"
             src="https://ven08761.service-now.com/x_enno_gatenow.GateNow%20Logo.png"
             alt="GateNow"
             onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />

        <span class="gn-brand-text-fallback" style="display:none;">
          <span class="gn-logo-mark" aria-hidden="true">
            <svg class="gn-logo-icon" viewBox="0 0 24 24" fill="none"
                 xmlns="http://www.w3.org/2000/svg">
              <path d="M4.5 11L12 4.5 19.5 11" stroke="white" stroke-width="1.8"
                    stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M6.5 9.5V18a1 1 0 001 1H10.5V15.2C10.5 14.5 11 14 11.7 14H12.3
                       C13 14 13.5 14.5 13.5 15.2V19H16.5A1 1 0 0017.5 18V9.5"
                    stroke="white" stroke-width="1.8"
                    stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </span>
          <span class="gn-brand-text">GateNow</span>
        </span>

      </a>

      <!-- Mobile hamburger -->
      <button type="button"
              class="navbar-toggle collapsed gn-hamburger"
              data-toggle="collapse"
              data-target="#sp-nav-bar"
              aria-haspopup="menu">
        <span class="sr-only">${Toggle navigation}</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

    </div>

    <!-- CENTER + RIGHT: nav items -->
    <div sp-navbar-toggle=""
         class="collapse navbar-right gn-navbar-right"
         id="sp-nav-bar"
         role="navigation">

      <!-- Center menu -->
      <ul class="gn-nav-menu" role="presentation">
        <li><a href="javascript:void(0);" ng-click="scrollToSection('about_gate', $event); collapse()">${About}</a></li>
        <li><a ng-href="?id=opportunities" ng-click="collapse()">${Programs}</a></li>
        <li><a href="javascript:void(0);" ng-click="scrollToSection('application_process', $event); collapse()">${Process}</a></li>
        <li><a href="javascript:void(0);" ng-click="scrollToSection('faq_applicant', $event); collapse()">${FAQ}</a></li>
      </ul>

      <!-- Language selector -->
      <sp-widget widget="::data.langSelector"></sp-widget>

      <!-- NOT LOGGED IN -->
      <ul ng-if="(!user.logged_in && page.id != portal.login_page_dv && !data.hasLogin)"
          class="nav navbar-nav gn-nav-actions"
          role="presentation">

        <li role="presentation">
          <a href ng-click="openLogin()" class="gn-signin-link">
            ${Sign in}
          </a>
        </li>

        <li role="presentation">
          <a ng-href="?id=application_status" class="gn-cta-btn">
            ${Application Status}
          </a>
        </li>
      </ul>

      <!-- LOGGED IN -->
      <ul ng-if="user.logged_in" class="nav navbar-nav gn-nav-actions">

        <!-- Live Chat -->
        <li ng-if="::(data.connect_support_queue_id && !isAgentChatConfigured)">
          <a href ng-click="openPopUp()" role="button" class="gn-nav-link">
            ${Live Chat}
          </a>
        </li>

        <!-- Apply for funding -->
        <li role="presentation">
          <a ng-href="?id=application_status" class="gn-cta-btn">
            ${Application Status}
          </a>
        </li>

        <!-- ── Desktop: avatar dropdown ── -->
        <li ng-if="showAvatar" class="hidden-xs hidden-sm dropdown gn-avatar-dropdown">
          <a href
             class="toggle-dropdown gn-avatar-btn"
             data-toggle="dropdown"
             aria-expanded="false"
             aria-label="{{::data.profileBtnMsg}}: {{::user.name}}"
             id="gn-profile-dropdown"
             role="button"
             aria-haspopup="true">
            <span class="navbar-avatar" aria-hidden="true">
              <sn-avatar class="avatar-small-medium" primary="avatarProfile"/>
            </span>
            <span class="visible-lg-inline gn-user-name">{{::user.name}}</span>
            <svg class="gn-chevron" viewBox="0 0 12 12" fill="none"
                 xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <path d="M2 4L6 8L10 4" stroke="#131B34" stroke-width="1.5"
                    stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>

          <ul class="dropdown-menu gn-dropdown-menu" aria-labelledby="gn-profile-dropdown">

            <li class="gn-dropdown-header" role="presentation">
              <span class="gn-dropdown-name">{{::user.name}}</span>
              <span class="gn-dropdown-role">${Applicant}</span>
            </li>
            <li class="gn-dropdown-divider" role="separator"></li>

            <li role="presentation">
              <a tabindex="-1"
                 ng-href="?id=user_profile&sys_id={{::user.sys_id}}"
                 class="gn-dropdown-item" role="menuitem">
                <svg viewBox="0 0 16 16" fill="none" width="14" height="14" aria-hidden="true">
                  <circle cx="8" cy="5" r="3" stroke="#2554EB" stroke-width="1.4"/>
                  <path d="M2 14c0-3.314 2.686-5 6-5s6 1.686 6 5"
                        stroke="#2554EB" stroke-width="1.4" stroke-linecap="round"/>
                </svg>
                ${Profile}
              </a>
            </li>
            <li class="gn-dropdown-divider" role="separator"></li>

            <li ng-if="::!(isViewNative || isViewNativeTablet)" role="presentation">
              <a tabindex="-1" ng-href="{{logoutUrl}}"
                 class="gn-dropdown-item gn-dropdown-logout" role="menuitem">
                <svg viewBox="0 0 16 16" fill="none" width="14" height="14" aria-hidden="true">
                  <path d="M6 14H3a1 1 0 01-1-1V3a1 1 0 011-1h3"
                        stroke="#EF4444" stroke-width="1.4" stroke-linecap="round"/>
                  <path d="M10 11l3-3-3-3M13 8H6"
                        stroke="#EF4444" stroke-width="1.4"
                        stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
                ${Logout}
              </a>
            </li>
          </ul>
        </li>

        <!-- ── Mobile: avatar link ── -->
        <li ng-if="showSMAvatar" class="visible-xs-block visible-sm-block">
          <a ng-href="?id=user_profile&sys_id={{::user.sys_id}}"
             ng-click="collapse()" class="gn-avatar-btn">
            <span class="navbar-avatar">
              <sn-avatar class="avatar-small-medium" primary="avatarProfile"/>
            </span>
            {{::user.name}}
          </a>
        </li>

        <!-- ── Mobile: logout ── -->
        <li ng-if="::!(isViewNative || isViewNativeTablet)"
            class="visible-xs-block visible-sm-block">
          <a ng-href="{{logoutUrl}}" class="gn-nav-link gn-nav-logout">
            <svg viewBox="0 0 16 16" fill="none" width="14" height="14" aria-hidden="true">
              <path d="M6 14H3a1 1 0 01-1-1V3a1 1 0 011-1h3"
                    stroke="#131B34" stroke-width="1.4" stroke-linecap="round"/>
              <path d="M10 11l3-3-3-3M13 8H6"
                    stroke="#131B34" stroke-width="1.4"
                    stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            ${Logout}
          </a>
        </li>

      </ul>

    </div>
  </div>
</nav>

<!-- Login modal -->
<script type="text/ng-template" id="modalLogin">
  <div class="modal-body">
    <sp-widget widget="data.loginWidget"></sp-widget>
  </div>
</script>

</div>]]></template>
    </sp_header_footer>
</record_update>

<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[api.controller = function($scope, $timeout, $window, $location, $element, spModal) {

  /* =========================================================
     GATENOW · APPLICANT INTAKE · Client script

     FIXES

     1. The flicker on Continue.
        nextStep() called saveDraft(), which called
        c.server.get(). Every server call re-runs the server
        script and replaces c.data, and Angular repaints the
        widget while that happens. What you saw as "a glimpse of
        the previous tab" was the widget rebuilding. Draft saving
        is gone, so Continue now only changes a number and
        nothing round trips.

     2. Uploads.
        The step used ng-file-upload (ngf-select / ngf-drop),
        which is not part of the Service Portal AngularJS bundle,
        so the directives never bound and nothing happened on
        click or drop. It now uses a native <input type="file">;
        this controller wires the change / drag / drop events
        itself once step 5 renders. The type check keeps its
        extension fallback, because a dragged or older .doc file
        often arrives with an empty or unexpected MIME type.

     3. Every field required.
        Middle name, organisation and the demographic selects are
        all validated. SSN is the single exception and is only
        required when the applicant type is Individual, since an
        organisation does not have one.
     ========================================================= */

  var c = this;

  var CONFIG = {
    MAX_FILE_SIZE: 10485760,
    TYPES: [
      'application/pdf',
      'application/msword',
      'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
      'image/jpeg', 'image/jpg', 'image/png'
    ],
    EXTS: ['.pdf', '.doc', '.docx', '.jpg', '.jpeg', '.png']
  };


  /* =====================================================
     DATES
     Parsed by hand so a value never shifts a day across a
     timezone boundary.
     ===================================================== */

  function toLocalDate(s) {
    if (!s || typeof s !== 'string') return s;
    var p = s.split('-');
    return p.length === 3 ? new Date(p[0], p[1] - 1, p[2]) : s;
  }

  function fromLocalDate(d) {
    if (!d || !(d instanceof Date) || isNaN(d)) return '';
    var m = d.getMonth() + 1;
    var day = d.getDate();
    return d.getFullYear() + '-' + (m < 10 ? '0' + m : m) + '-' + (day < 10 ? '0' + day : day);
  }

  function dateText(v) {
    if (!v) return '';
    return (v instanceof Date) ? fromLocalDate(v) : v;
  }


  /* =====================================================
     STATE
     ===================================================== */

  c.steps = [
    { n: 1, label: c.data.t.s1, note: c.data.t.s1n },
    { n: 2, label: c.data.t.s2, note: c.data.t.s2n },
    { n: 3, label: c.data.t.s3, note: c.data.t.s3n },
    { n: 4, label: c.data.t.s4, note: c.data.t.s4n },
    { n: 5, label: c.data.t.s5, note: c.data.t.s5n },
    { n: 6, label: c.data.t.s6, note: c.data.t.s6n }
  ];

  c.totalSteps     = c.steps.length;
  c.currentStep    = 1;
  c.completedSteps = [];
  c.errors         = {};
  c.saving         = false;
  c.uploadedFiles  = [];
  c.uploadProgress = 0;
  c.isDragging     = false;
  c.formBlocked    = false;
  c.draftResumed   = false;
  c.successBanner  = false;
  c.successMessage = '';
  c.countdown      = 5;
  c.confirmed      = false;

  c.demoFields = [
    { key: 'gender',             label: c.data.t.gender },
    { key: 'race',               label: c.data.t.race },
    { key: 'ethnicity',          label: c.data.t.ethnicity },
    { key: 'education_level',    label: c.data.t.education },
    { key: 'marital_status',     label: c.data.t.marital },
    { key: 'military_status',    label: c.data.t.military },
    { key: 'disability_status',  label: c.data.t.disability },
    { key: 'tribal_affiliation', label: c.data.t.tribal }
  ];

  c.form = angular.merge({
    first_name: '', middle_name: '', last_name: '',
    email: '', phone_number: '', country: '',
    city: '', street: '', zip_postal_code: '',
    ssn: '', race: '', birthdate: '',
    uei: '', tin_ein: '',
    applicant_type: '', identity_type: '',
    gender: '', ethnicity: '',
    education_level: '', marital_status: '',
    military_status: '', disability_status: '',
    citizenship_status: '', tribal_affiliation: '',
    grant_program: '', program_category: '',
    proposal_title: '', short_description: '',
    proposal_description: '',
    project_goals_and_objectives: '',
    project_activities_methodology: '',
    organization: '',
    requested_budget: null,
    project_start_date: '', project_end_date: ''
  }, c.data.prefill || {});

  if (c.form.birthdate)          c.form.birthdate          = toLocalDate(c.form.birthdate);
  if (c.form.project_start_date) c.form.project_start_date = toLocalDate(c.form.project_start_date);
  if (c.form.project_end_date)   c.form.project_end_date   = toLocalDate(c.form.project_end_date);

  if (c.data.alreadySubmitted) c.formBlocked = true;

  if (!c.formBlocked && c.data.applicant_sys_id &&
      c.data.prefill && c.data.prefill.first_name) {
    c.draftResumed = true;
  }

  /* the program comes from the saved record first, then the URL */
  var target = (c.data.prefill && c.data.prefill.grant_program)
                 ? c.data.prefill.grant_program
                 : (c.data.program_param || '');

  $timeout(function() {
    if (target) c.form.grant_program = String(target).trim();
  });

  /* category is derived from the program, never typed */
  $scope.$watch(function() { return c.form.grant_program; }, function(val) {
    if (!val) { c.form.program_category = ''; return; }

    var list = c.data.programs || [];
    for (var i = 0; i < list.length; i++) {
      if (list[i].sys_id === val) {
        c.form.program_category = list[i].category || '';
        return;
      }
    }

    c.form.program_category = (c.data.prefill && c.data.prefill.program_category) || '';
  });


  /* =====================================================
     RAIL
     ===================================================== */

  c.stepTitle = function() {
    for (var i = 0; i < c.steps.length; i++) {
      if (c.steps[i].n === c.currentStep) return c.steps[i].label;
    }
    return '';
  };

  c.stepHint = function() {
    var h = [c.data.t.h1, c.data.t.h2, c.data.t.h3, c.data.t.h4, c.data.t.h5, c.data.t.h6];
    return h[c.currentStep - 1] || '';
  };

  c.isDone = function(n) { return c.completedSteps.indexOf(n) !== -1; };

  /* One predicate drives both the tick and the number, so they can
     never appear together. A step shows a tick once it is completed
     AND you have moved off it; the step you are standing on always
     shows its number, even after it has been completed. */
  c.stepShowsCheck = function(n) {
    return c.isDone(n) && c.currentStep !== n;
  };

  c.progress = function() {
    return Math.round((c.completedSteps.length / c.totalSteps) * 100);
  };

  c.errorCount = function() {
    var n = 0;
    for (var k in c.errors) { if (c.errors.hasOwnProperty(k)) n++; }
    return n;
  };

  c.left = function(val, max) {
    return (max - (val || '').length) + ' ' + c.data.t.charsLeft;
  };

  function programField(field) {
    var list = c.data.programs || [];
    for (var i = 0; i < list.length; i++) {
      if (list[i].sys_id === c.form.grant_program) return list[i][field] || '';
    }
    return '';
  }

  c.programName = function() { return programField('name'); };

  c.programMeta = function() {
    var bits = [];
    var num = programField('number');
    var cat = programField('category');
    if (num) bits.push(num);
    if (cat) bits.push(cat);
    return bits.join('  ·  ');
  };


  /* =====================================================
     VALIDATION
     Everything is required. SSN is the one exception, asked
     only of individual applicants.
     ===================================================== */

  function empty(v) {
    return v === undefined || v === null || v === '' ||
           (typeof v === 'string' && v.trim() === '');
  }

  c.validateStep = function(step) {
    c.errors = {};
    var ok = true;

    function need(field, msg) {
      if (empty(c.form[field])) { c.errors[field] = msg; ok = false; }
    }

    if (step === 1) {
      need('first_name', c.data.t.eFirst);
      need('middle_name', c.data.t.eMiddle);
      need('last_name', c.data.t.eLast);

      if (empty(c.form.email)) { c.errors.email = c.data.t.eEmail; ok = false; }
      else if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(c.form.email)) {
        c.errors.email = c.data.t.eEmailBad; ok = false;
      }

      need('phone_number', c.data.t.ePhone);
      need('birthdate', c.data.t.eDob);
      need('street', c.data.t.eStreet);
      need('city', c.data.t.eCity);
      need('zip_postal_code', c.data.t.eZip);
      need('country', c.data.t.eCountry);
    }

    else if (step === 2) {
      need('citizenship_status', c.data.t.eCitizen);

      /* An organisation does not have a social security number,
         so it is only demanded of individuals. Applicant type is
         chosen on step three, so a blank type does not block
         this step. */
      if (c.form.applicant_type === 'Individual') {
        need('ssn', c.data.t.eSsn);
      }

      for (var i = 0; i < c.demoFields.length; i++) {
        need(c.demoFields[i].key,
             c.demoFields[i].label + ' ' + c.data.t.eIsRequired);
      }
    }

    else if (step === 3) {
      need('grant_program', c.data.t.eProgram);
      need('applicant_type', c.data.t.eAType);
      need('organization', c.data.t.eOrg);
      need('identity_type', c.data.t.eIType);
      need('uei', c.data.t.eUei);
      need('tin_ein', c.data.t.eTin);
    }

    else if (step === 4) {
      need('proposal_title', c.data.t.eTitle);
      need('short_description', c.data.t.eShort);
      need('proposal_description', c.data.t.eDesc);
      need('project_goals_and_objectives', c.data.t.eGoals);
      need('project_activities_methodology', c.data.t.eMethod);

      if (!c.form.requested_budget || c.form.requested_budget <= 0) {
        c.errors.requested_budget = c.data.t.eBudget; ok = false;
      }

      need('project_start_date', c.data.t.eStart);
      need('project_end_date', c.data.t.eEnd);

      if (c.form.project_start_date && c.form.project_end_date) {
        if (new Date(c.form.project_end_date) <= new Date(c.form.project_start_date)) {
          c.errors.project_end_date = c.data.t.eOrder; ok = false;
        }
      }
    }

    else if (step === 5) {
      if (!c.uploadedFiles.length) {
        c.errors.documents = c.data.t.eDocs; ok = false;
      }
    }

    else if (step === 6) {
      if (!c.uploadedFiles.length) {
        c.errors.documents = c.data.t.eDocs; ok = false;
      }
      if (!c.confirmed) {
        c.errors.confirmed = c.data.t.eConfirm; ok = false;
      }
    }

    if (!ok) scrollToErrors();

    return ok;
  };

  /* scoped to this widget rather than the whole document, so a
     second widget on the page cannot be scrolled to by mistake */
  function scrollToErrors() {
    $timeout(function() {
      var box = $element[0].querySelector('.gni-errors');
      if (box && box.scrollIntoView) {
        box.scrollIntoView({ behavior: 'smooth', block: 'center' });
      }
    }, 80);
  }


  /* =====================================================
     NAVIGATION
     No server call here. That was the flicker.
     ===================================================== */

  function toTop() {
    $timeout(function() {
      $window.scrollTo({ top: 0, behavior: 'smooth' });
    }, 60);
  }

  function markDone(step) {
    if (c.completedSteps.indexOf(step) === -1) c.completedSteps.push(step);
  }

  c.nextStep = function() {
    if (c.saving) return;
    if (!c.validateStep(c.currentStep)) return;
    markDone(c.currentStep);
    c.currentStep++;
    c.errors = {};
    toTop();
  };

  c.prevStep = function() {
    if (c.currentStep <= 1 || c.saving) return;
    c.errors = {};
    c.currentStep--;
    toTop();
  };

  c.goToStep = function(step) {
    if (c.formBlocked || c.saving) return;
    if (step < 1 || step > c.totalSteps || step === c.currentStep) return;

    /* back is always allowed, forward has to pass the step you
       are standing on */
    if (step > c.currentStep) {
      if (!c.validateStep(c.currentStep)) return;
      markDone(c.currentStep);
    }

    c.errors = {};
    c.currentStep = step;
    toTop();
  };


  /* =====================================================
     REVIEW
     ===================================================== */

  function row(label, value) { return { label: label, value: value }; }

  function choiceLabel(field, value) {
    var list = (c.data.choices || {})[field] || [];
    for (var i = 0; i < list.length; i++) {
      if (list[i].value === value) return list[i].label;
    }
    return value || '';
  }

  function money(v) {
    if (!v) return '';
    var s = '' + Math.round(v);
    var out = '';
    var n = 0;
    for (var i = s.length - 1; i >= 0; i--) {
      out = s.charAt(i) + out;
      n++;
      if (n % 3 === 0 && i > 0) out = ',' + out;
    }
    return '$' + out;
  }

  c.recap = function() {
    var f = c.form;

    return [
      {
        step: 1,
        title: c.data.t.s1,
        rows: [
          row(c.data.t.firstName, [f.first_name, f.middle_name, f.last_name].join(' ').replace(/\s+/g, ' ').trim()),
          row(c.data.t.email, f.email),
          row(c.data.t.phone, f.phone_number),
          row(c.data.t.birthdate, dateText(f.birthdate)),
          row(c.data.t.address, [f.street, f.city, f.zip_postal_code, f.country].filter(Boolean).join(', '))
        ]
      },
      {
        step: 2,
        title: c.data.t.s2,
        rows: [
          row(c.data.t.citizenship, choiceLabel('citizenship_status', f.citizenship_status)),
          row(c.data.t.gender, choiceLabel('gender', f.gender)),
          row(c.data.t.race, choiceLabel('race', f.race)),
          row(c.data.t.ethnicity, choiceLabel('ethnicity', f.ethnicity)),
          row(c.data.t.education, choiceLabel('education_level', f.education_level)),
          row(c.data.t.marital, choiceLabel('marital_status', f.marital_status)),
          row(c.data.t.military, choiceLabel('military_status', f.military_status)),
          row(c.data.t.disability, choiceLabel('disability_status', f.disability_status)),
          row(c.data.t.tribal, choiceLabel('tribal_affiliation', f.tribal_affiliation))
        ]
      },
      {
        step: 3,
        title: c.data.t.s3,
        rows: [
          row(c.data.t.program, c.programName()),
          row(c.data.t.category, f.program_category),
          row(c.data.t.applicantType, choiceLabel('applicant_type', f.applicant_type)),
          row(c.data.t.organisation, f.organization),
          row(c.data.t.identityType, choiceLabel('identity_type', f.identity_type)),
          row(c.data.t.uei, f.uei),
          row(c.data.t.tin, f.tin_ein)
        ]
      },
      {
        step: 4,
        title: c.data.t.s4,
        rows: [
          row(c.data.t.proposalTitle, f.proposal_title),
          row(c.data.t.shortDesc, f.short_description),
          row(c.data.t.budget, money(f.requested_budget)),
          row(c.data.t.startDate, dateText(f.project_start_date)),
          row(c.data.t.endDate, dateText(f.project_end_date))
        ]
      }
    ];
  };


  /* =====================================================
     FILES
     ===================================================== */

  /* A dragged file, and an older .doc, often arrive with an
     empty or unexpected type, which a strict MIME check rejects
     without saying why. The extension is the fallback. */
  function typeOk(file) {
    if (file.type && CONFIG.TYPES.indexOf(file.type) !== -1) return true;

    var name = (file.name || '').toLowerCase();
    for (var i = 0; i < CONFIG.EXTS.length; i++) {
      var ext = CONFIG.EXTS[i];
      if (name.length > ext.length && name.slice(-ext.length) === ext) return true;
    }
    return false;
  }

  function alreadyAdded(file) {
    for (var i = 0; i < c.uploadedFiles.length; i++) {
      var f = c.uploadedFiles[i];
      if (f.name === file.name && f.size === file.size) return true;
    }
    return false;
  }

  c.addFiles = function($files) {
    if (!$files || !$files.length) return;

    angular.forEach($files, function(file) {
      if (!file) return;

      if (file.size > CONFIG.MAX_FILE_SIZE) {
        spModal.open({
          title: c.data.t.tooBigTitle,
          message: '"' + file.name + '" ' + c.data.t.tooBigText,
          buttons: [{ label: c.data.t.ok, primary: true }]
        });
        return;
      }

      if (!typeOk(file)) {
        spModal.open({
          title: c.data.t.badTypeTitle,
          message: '"' + file.name + '" ' + c.data.t.badTypeText,
          buttons: [{ label: c.data.t.ok, primary: true }]
        });
        return;
      }

      /* choosing the same file twice would attach it twice */
      if (alreadyAdded(file)) return;

      c.uploadedFiles.push(file);
    });

    if (c.uploadedFiles.length) delete c.errors.documents;
  };

  c.removeFile = function(i) {
    if (i >= 0 && i < c.uploadedFiles.length) c.uploadedFiles.splice(i, 1);
  };

  c.formatFileSize = function(bytes) {
    if (!bytes) return '0 B';
    var k = 1024;
    var units = ['B', 'KB', 'MB', 'GB'];
    var i = Math.floor(Math.log(bytes) / Math.log(k));
    return Math.round(bytes / Math.pow(k, i) * 10) / 10 + ' ' + units[i];
  };

  function toBase64(files) {
    var jobs = [];

    angular.forEach(files, function(file) {
      jobs.push(new Promise(function(resolve, reject) {
        var reader = new FileReader();

        reader.onload = function(e) {
          try {
            var parts = ('' + e.target.result).split(',');
            resolve({
              name: file.name,
              /* the server needs a type, and a dragged file may
                 not have one */
              type: file.type || 'application/octet-stream',
              size: file.size,
              data: parts.length > 1 ? parts[1] : parts[0]
            });
          } catch (err) { reject(err); }
        };

        reader.onerror = function() { reject('read failed: ' + file.name); };
        reader.readAsDataURL(file);
      }));
    });

    return Promise.all(jobs);
  }


  /* =====================================================
     FILE INPUT + DROPZONE WIRING
     Replaces ng-file-upload, which Service Portal does not
     bundle. Step 5 lives inside an ng-if, so its DOM is built
     fresh each time the step opens; the listeners are therefore
     (re)attached whenever step 5 renders. The freshly-created
     element carries no bound flag, so it wires exactly once.
     ===================================================== */

  c.pickFiles = function() {
    var input = $element[0].querySelector('.gni-file-input');
    if (input) input.click();
  };

  function bindDropzone() {
    var drop  = $element[0].querySelector('.gni-drop');
    var input = $element[0].querySelector('.gni-file-input');
    if (!drop || !input) return;

    if (drop.getAttribute('data-gni-bound') === '1') return;
    drop.setAttribute('data-gni-bound', '1');

    /* native picker */
    input.addEventListener('change', function(e) {
      var files = e.target.files;
      $scope.$apply(function() { c.addFiles(files); });
      /* clear so the same file can be re-picked after removal */
      e.target.value = '';
    });

    /* drag and drop */
    drop.addEventListener('dragover', function(e) {
      e.preventDefault();
      e.stopPropagation();
      if (!c.isDragging) $scope.$apply(function() { c.isDragging = true; });
    });

    drop.addEventListener('dragleave', function(e) {
      e.preventDefault();
      e.stopPropagation();
      if (c.isDragging) $scope.$apply(function() { c.isDragging = false; });
    });

    drop.addEventListener('drop', function(e) {
      e.preventDefault();
      e.stopPropagation();
      var files = (e.dataTransfer && e.dataTransfer.files) ? e.dataTransfer.files : null;
      $scope.$apply(function() {
        c.isDragging = false;
        if (files && files.length) c.addFiles(files);
      });
    });
  }

  $scope.$watch(function() { return c.currentStep; }, function(step) {
    if (step === 5) $timeout(bindDropzone);
  });


  /* =====================================================
     SUBMIT
     ===================================================== */

  function payload() {
    var p = angular.copy(c.form);

    p.birthdate          = dateText(p.birthdate);
    p.project_start_date = dateText(p.project_start_date);
    p.project_end_date   = dateText(p.project_end_date);

    p.action = 'save_applicant';
    p.applicant_sys_id = c.data.applicant_sys_id || '';

    return p;
  }

  c.submit = function() {
    if (c.saving) return;
    if (!c.validateStep(6)) return;

    c.saving = true;
    c.uploadProgress = 10;

    var p = payload();

    if (c.uploadedFiles.length) {
      toBase64(c.uploadedFiles).then(function(files) {
        /* the promise resolves outside a digest */
        $timeout(function() {
          p.attachments = files;
          c.uploadProgress = 55;
          send(p);
        });
      }, function() {
        $timeout(function() {
          c.saving = false;
          c.uploadProgress = 0;
          spModal.open({
            title: c.data.t.fileErrTitle,
            message: c.data.t.fileErrText,
            buttons: [{ label: c.data.t.ok, primary: true }]
          });
        });
      });
    } else {
      send(p);
    }
  };

  function send(p) {
    c.server.get(p).then(function(r) {
      c.uploadProgress = 100;
      var resp = (r && r.data) ? r.data : {};

      if (resp.success) {
        done(c.data.t.submitted, resp.error);
      } else if (resp.alreadySubmitted) {
        c.formBlocked = true;
        done(c.data.t.already, '');
      } else {
        c.saving = false;
        c.uploadProgress = 0;
        spModal.open({
          title: c.data.t.submitErrTitle,
          message: resp.error || c.data.t.submitErrText,
          buttons: [{ label: c.data.t.ok, primary: true }]
        });
      }
    }, function() {
      c.saving = false;
      c.uploadProgress = 0;
      spModal.open({
        title: c.data.t.connErrTitle,
        message: c.data.t.connErrText,
        buttons: [{ label: c.data.t.ok, primary: true }]
      });
    });
  }

  /* warn is set when the record saved but the attachments did
     not, so the applicant is told rather than left assuming */
  function done(msg, warn) {
    c.successBanner  = true;
    c.successMessage = msg;
    c.successWarning = warn || '';
    c.uploadProgress = 0;
    c.countdown = warn ? 12 : 5;

    $timeout(function() {
      $window.scrollTo({ top: 0, behavior: 'smooth' });
    }, 50);

    (function tick() {
      $timeout(function() {
        c.countdown--;
        if (c.countdown > 0) tick();
        else $location.url(c.data.dashboardUrl);
      }, 1000);
    })();
  }


  /* =====================================================
     CANCEL
     ===================================================== */

  c.cancel = function() {
    spModal.open({
      title: c.data.t.cancelTitle,
      message: c.data.t.cancelText,
      buttons: [
        { label: c.data.t.keepGoing, cancel: true },
        { label: c.data.t.discard, primary: true }
      ]
    }).then(function() {
      $location.url(c.data.dashboardUrl);
    });
  };

};]]></client_script>
        <controller_as>c</controller_as>
        <css>/* =========================================================&#13;
   GATENOW · APPLICANT INTAKE&#13;
   Complete widget stylesheet (SCSS)&#13;
&#13;
   This REPLACES the widget's CSS field in full. Paste the whole&#13;
   block; do not keep the old partial patch alongside it.&#13;
&#13;
   It styles every class the HTML template emits, and folds in the&#13;
   four fixes from the patch (money alignment, the required note,&#13;
   the success-screen warning, the scrollable file list). The dead&#13;
   .gni-opt / .gni-saved rules are simply gone.&#13;
&#13;
   THEME&#13;
   All colour/shape decisions live in the token block below as CSS&#13;
   custom properties. To share one look across every GateNow widget,&#13;
   lift that block into the Service Portal Theme's CSS as :root { ... }&#13;
   and delete it here — this sheet will then inherit it. Palette is&#13;
   the navy (#0a2540) you were already using, plus the amber warning&#13;
   tone, a success green, and an error red.&#13;
   ========================================================= */&#13;
&#13;
.gni {&#13;
&#13;
  /* ---------- theme tokens (lift to :root to share) ---------- */&#13;
  --gni-ink:        #0a2540;                 // primary navy&#13;
  --gni-ink-hover:  #14395e;                 // navy, one step lighter&#13;
  --gni-ink-90:     rgba(10, 37, 64, 0.90);&#13;
  --gni-ink-70:     rgba(10, 37, 64, 0.70);&#13;
  --gni-ink-54:     rgba(10, 37, 64, 0.54);&#13;
  --gni-ink-40:     rgba(10, 37, 64, 0.40);&#13;
  --gni-ink-16:     rgba(10, 37, 64, 0.16);&#13;
  --gni-ink-12:     rgba(10, 37, 64, 0.12);&#13;
  --gni-ink-10:     rgba(10, 37, 64, 0.10);&#13;
  --gni-ink-06:     rgba(10, 37, 64, 0.06);&#13;
  --gni-ink-04:     rgba(10, 37, 64, 0.04);&#13;
&#13;
  // interactive blue — buttons, active step, progress, focus, links.&#13;
  // Text stays --gni-ink (above) so it keeps contrast. To go lighter&#13;
  // or darker, change these three; everything reroutes from here.&#13;
  --gni-brand:      #2563eb;                  // primary blue&#13;
  --gni-brand-hover:#1d4ed8;                  // one step darker (hover)&#13;
  --gni-brand-16:   rgba(37, 99, 235, 0.16);&#13;
  --gni-brand-10:   rgba(37, 99, 235, 0.10);&#13;
  --gni-brand-08:   rgba(37, 99, 235, 0.08);&#13;
&#13;
  --gni-ok:         #1f8a52;                  // success green&#13;
  --gni-ok-bg:      rgba(31, 138, 82, 0.10);&#13;
&#13;
  --gni-warn:       #8a6119;                  // amber (from the patch)&#13;
  --gni-warn-bd:    rgba(164, 116, 31, 0.30);&#13;
  --gni-warn-bg:    rgba(164, 116, 31, 0.09);&#13;
&#13;
  --gni-err:        #c8372d;                  // error red&#13;
  --gni-err-bd:     rgba(200, 55, 45, 0.40);&#13;
  --gni-err-bg:     rgba(200, 55, 45, 0.07);&#13;
&#13;
  --gni-surface:    #ffffff;&#13;
  --gni-canvas:     #f4f6f9;                  // page behind the card&#13;
  --gni-line:       rgba(10, 37, 64, 0.12);&#13;
&#13;
  --gni-r-sm: 7px;&#13;
  --gni-r-md: 11px;&#13;
  --gni-r-lg: 16px;&#13;
&#13;
  --gni-shadow: 0 1px 2px rgba(10, 37, 64, 0.06),&#13;
                0 10px 28px rgba(10, 37, 64, 0.06);&#13;
&#13;
  /* ---------- root / reset ---------- */&#13;
  color: var(--gni-ink);&#13;
  font-family: inherit;                      // use the portal's font&#13;
  -webkit-font-smoothing: antialiased;&#13;
  background: var(--gni-canvas);             // remove if the page already paints one&#13;
  padding: 24px 16px 40px;&#13;
&#13;
  *, *::before, *::after { box-sizing: border-box; }&#13;
  button { font-family: inherit; }&#13;
&#13;
  /* =======================================================&#13;
     STATE SCREENS · blocked + success&#13;
     ======================================================= */&#13;
  .gni-block,&#13;
  .gni-done {&#13;
    max-width: 560px;&#13;
    margin: 40px auto;&#13;
    padding: 40px 32px;&#13;
    text-align: center;&#13;
    background: var(--gni-surface);&#13;
    border: 1px solid var(--gni-line);&#13;
    border-radius: var(--gni-r-lg);&#13;
    box-shadow: var(--gni-shadow);&#13;
  }&#13;
&#13;
  .gni-block__mark,&#13;
  .gni-done__mark {&#13;
    display: grid;&#13;
    place-items: center;&#13;
    width: 56px;&#13;
    height: 56px;&#13;
    margin: 0 auto 20px;&#13;
    border-radius: 50%;&#13;
    font-size: 24px;&#13;
  }&#13;
  .gni-block__mark { background: var(--gni-ink-06); color: var(--gni-ink); }&#13;
  .gni-done__mark  { background: var(--gni-brand-08); color: var(--gni-brand); }&#13;
&#13;
  .gni-block__title,&#13;
  .gni-done__title {&#13;
    margin: 0 0 10px;&#13;
    font-size: 22px;&#13;
    font-weight: 700;&#13;
    line-height: 1.25;&#13;
  }&#13;
&#13;
  .gni-block__text,&#13;
  .gni-done__text {&#13;
    margin: 0 0 22px;&#13;
    font-size: 14px;&#13;
    line-height: 1.6;&#13;
    color: var(--gni-ink-70);&#13;
  }&#13;
  .gni-done__text strong { color: var(--gni-ink); font-weight: 700; }&#13;
&#13;
  /* success warning — record saved, attachments did not (patch #4) */&#13;
  .gni-done__warn {&#13;
    margin: 14px 0 22px;&#13;
    padding: 12px 16px;&#13;
    border: 1px solid var(--gni-warn-bd);&#13;
    border-radius: var(--gni-r-md);&#13;
    background: var(--gni-warn-bg);&#13;
    font-size: 13.5px;&#13;
    line-height: 1.6;&#13;
    color: var(--gni-warn);&#13;
    text-align: left;&#13;
  }&#13;
&#13;
  /* =======================================================&#13;
     LAYOUT · wrap, context header, body&#13;
     ======================================================= */&#13;
  .gni-wrap { max-width: 1080px; margin: 0 auto; }&#13;
&#13;
  .gni-context {&#13;
    display: flex;&#13;
    align-items: flex-end;&#13;
    justify-content: space-between;&#13;
    gap: 16px;&#13;
    margin: 0 0 24px;&#13;
  }&#13;
  .gni-context__label {&#13;
    font-size: 11px;&#13;
    font-weight: 700;&#13;
    letter-spacing: 0.08em;&#13;
    text-transform: uppercase;&#13;
    color: var(--gni-ink-54);&#13;
  }&#13;
  .gni-context__name {&#13;
    margin: 4px 0 0;&#13;
    font-size: 22px;&#13;
    font-weight: 700;&#13;
    line-height: 1.2;&#13;
  }&#13;
  .gni-context__meta {&#13;
    margin: 4px 0 0;&#13;
    font-size: 13px;&#13;
    color: var(--gni-ink-54);&#13;
  }&#13;
&#13;
  .gni-body {&#13;
    display: grid;&#13;
    grid-template-columns: 264px 1fr;&#13;
    gap: 28px;&#13;
    align-items: start;&#13;
  }&#13;
&#13;
  /* =======================================================&#13;
     STEP RAIL + PROGRESS&#13;
     ======================================================= */&#13;
  .gni-rail { position: sticky; top: 16px; }&#13;
&#13;
  .gni-rail__label {&#13;
    margin: 0 0 10px;&#13;
    font-size: 11px;&#13;
    font-weight: 700;&#13;
    letter-spacing: 0.08em;&#13;
    text-transform: uppercase;&#13;
    color: var(--gni-ink-54);&#13;
  }&#13;
&#13;
  .gni-steps {&#13;
    margin: 0;&#13;
    padding: 0;&#13;
    list-style: none;&#13;
    display: flex;&#13;
    flex-direction: column;&#13;
    gap: 4px;&#13;
  }&#13;
&#13;
  .gni-step {&#13;
    display: flex;&#13;
    align-items: center;&#13;
    gap: 12px;&#13;
    width: 100%;&#13;
    padding: 10px;&#13;
    text-align: left;&#13;
    background: none;&#13;
    border: none;&#13;
    border-radius: var(--gni-r-md);&#13;
    cursor: pointer;&#13;
    transition: background 0.15s;&#13;
&#13;
    &amp;:hover { background: var(--gni-ink-04); }&#13;
    &amp;:focus-visible { outline: none; box-shadow: 0 0 0 3px var(--gni-brand-16); }&#13;
  }&#13;
  .gni-step__mark {&#13;
    flex: 0 0 28px;&#13;
    height: 28px;&#13;
    display: grid;&#13;
    place-items: center;&#13;
    border-radius: 50%;&#13;
    border: 1.5px solid var(--gni-line);&#13;
    background: var(--gni-surface);&#13;
    color: var(--gni-ink-54);&#13;
    font-size: 12px;&#13;
    font-weight: 700;&#13;
  }&#13;
  .gni-step__body { display: flex; flex-direction: column; min-width: 0; }&#13;
  .gni-step__name { font-size: 13.5px; font-weight: 600; color: var(--gni-ink-70); }&#13;
  .gni-step__note { font-size: 12px; color: var(--gni-ink-40); }&#13;
&#13;
  .gni-step.is-on {&#13;
    background: var(--gni-brand-08);&#13;
    .gni-step__mark { background: var(--gni-brand); border-color: var(--gni-brand); color: #fff; }&#13;
    .gni-step__name { color: var(--gni-ink); }&#13;
  }&#13;
  .gni-step.is-done .gni-step__mark {&#13;
    background: var(--gni-brand);&#13;
    border-color: var(--gni-brand);&#13;
    color: #fff;&#13;
  }&#13;
  .gni-step.is-ahead { opacity: 0.72; }&#13;
&#13;
  .gni-rail__foot {&#13;
    margin-top: 16px;&#13;
    padding-top: 16px;&#13;
    border-top: 1px solid var(--gni-line);&#13;
  }&#13;
  .gni-rail__track {&#13;
    display: block;&#13;
    height: 6px;&#13;
    border-radius: 999px;&#13;
    background: var(--gni-ink-10);&#13;
    overflow: hidden;&#13;
  }&#13;
  .gni-rail__fill {&#13;
    display: block;&#13;
    height: 100%;&#13;
    border-radius: 999px;&#13;
    background: var(--gni-brand);&#13;
    transition: width 0.3s ease;&#13;
  }&#13;
  .gni-rail__pct {&#13;
    display: block;&#13;
    margin-top: 8px;&#13;
    font-size: 12px;&#13;
    font-weight: 600;&#13;
    color: var(--gni-ink-54);&#13;
  }&#13;
&#13;
  /* =======================================================&#13;
     PANEL&#13;
     ======================================================= */&#13;
  .gni-panel {&#13;
    padding: 28px 30px;&#13;
    background: var(--gni-surface);&#13;
    border: 1px solid var(--gni-line);&#13;
    border-radius: var(--gni-r-lg);&#13;
    box-shadow: var(--gni-shadow);&#13;
  }&#13;
&#13;
  .gni-resumed {&#13;
    display: flex;&#13;
    align-items: center;&#13;
    gap: 8px;&#13;
    margin: 0 0 18px;&#13;
    padding: 10px 14px;&#13;
    border: 1px solid var(--gni-ink-10);&#13;
    border-radius: var(--gni-r-md);&#13;
    background: var(--gni-ink-04);&#13;
    font-size: 13px;&#13;
    color: var(--gni-ink-70);&#13;
&#13;
    i { color: var(--gni-ink-54); }&#13;
  }&#13;
&#13;
  .gni-panel__head {&#13;
    margin-bottom: 22px;&#13;
    padding-bottom: 20px;&#13;
    border-bottom: 1px solid var(--gni-line);&#13;
  }&#13;
  .gni-panel__count {&#13;
    font-size: 11px;&#13;
    font-weight: 700;&#13;
    letter-spacing: 0.08em;&#13;
    text-transform: uppercase;&#13;
    color: var(--gni-ink-40);&#13;
  }&#13;
  .gni-panel__title { margin: 6px 0 0; font-size: 20px; font-weight: 700; }&#13;
  .gni-panel__hint  { margin: 6px 0 0; font-size: 13.5px; line-height: 1.55; color: var(--gni-ink-70); }&#13;
&#13;
  /* "every field is required" note (patch #3) */&#13;
  .gni-panel__req {&#13;
    display: inline-flex;&#13;
    align-items: center;&#13;
    margin: 14px 0 0;&#13;
    padding: 5px 11px;&#13;
    border-radius: var(--gni-r-sm);&#13;
    background: var(--gni-ink-04);&#13;
    font-size: 12px;&#13;
    font-weight: 500;&#13;
    color: var(--gni-ink-54);&#13;
  }&#13;
&#13;
  /* error summary box */&#13;
  .gni-errors {&#13;
    margin: 0 0 20px;&#13;
    padding: 14px 16px;&#13;
    border: 1px solid var(--gni-err-bd);&#13;
    border-radius: var(--gni-r-md);&#13;
    background: var(--gni-err-bg);&#13;
  }&#13;
  .gni-errors__title { margin: 0 0 8px; font-size: 13.5px; font-weight: 700; color: var(--gni-err); }&#13;
  .gni-errors__list  { margin: 0; padding-left: 18px; }&#13;
  .gni-errors__list li { font-size: 13px; line-height: 1.5; color: var(--gni-err); }&#13;
&#13;
  /* =======================================================&#13;
     FIELDS · 12-column grid&#13;
     ======================================================= */&#13;
  .gni-fields {&#13;
    display: grid;&#13;
    grid-template-columns: repeat(12, 1fr);&#13;
    gap: 18px 16px;&#13;
    align-items: start;&#13;
  }&#13;
&#13;
  .gni-field         { display: flex; flex-direction: column; grid-column: span 12; }&#13;
  .gni-field--full   { grid-column: span 12; }&#13;
  .gni-field--half   { grid-column: span 6; }&#13;
  .gni-field--third  { grid-column: span 4; }&#13;
  .gni-field--quarter{ grid-column: span 3; }&#13;
&#13;
  .gni-field label {&#13;
    margin-bottom: 6px;&#13;
    font-size: 13px;&#13;
    font-weight: 600;&#13;
    color: var(--gni-ink-70);&#13;
  }&#13;
&#13;
  .gni-field input,&#13;
  .gni-field select,&#13;
  .gni-field textarea {&#13;
    width: 100%;&#13;
    padding: 10px 12px;&#13;
    font-size: 14px;&#13;
    line-height: 1.4;&#13;
    color: var(--gni-ink);&#13;
    background: var(--gni-surface);&#13;
    border: 1px solid var(--gni-line);&#13;
    border-radius: var(--gni-r-sm);&#13;
    transition: border-color 0.15s, box-shadow 0.15s;&#13;
&#13;
    &amp;::placeholder { color: var(--gni-ink-40); }&#13;
&#13;
    &amp;:focus {&#13;
      outline: none;&#13;
      border-color: var(--gni-brand);&#13;
      box-shadow: 0 0 0 3px var(--gni-brand-10);&#13;
    }&#13;
&#13;
    &amp;.has-error {&#13;
      border-color: var(--gni-err-bd);&#13;
      &amp;:focus { box-shadow: 0 0 0 3px var(--gni-err-bg); }&#13;
    }&#13;
  }&#13;
&#13;
  .gni-field textarea { min-height: 96px; resize: vertical; }&#13;
&#13;
  /* select wrapper + chevron */&#13;
  .gni-pick { position: relative; }&#13;
  .gni-pick select {&#13;
    appearance: none;&#13;
    -webkit-appearance: none;&#13;
    padding-right: 36px;&#13;
    cursor: pointer;&#13;
  }&#13;
  .gni-pick i {&#13;
    position: absolute;&#13;
    top: 50%;&#13;
    right: 14px;&#13;
    transform: translateY(-50%);&#13;
    color: var(--gni-ink-54);&#13;
    font-size: 14px;&#13;
    pointer-events: none;&#13;
  }&#13;
&#13;
  /* money field (patch #1 — sign at 14, padding at 32) */&#13;
  .gni-money { position: relative; }&#13;
  .gni-money__sign {&#13;
    position: absolute;&#13;
    top: 50%;&#13;
    left: 14px;&#13;
    transform: translateY(-50%);&#13;
    color: var(--gni-ink-54);&#13;
    font-size: 14px;&#13;
    pointer-events: none;&#13;
  }&#13;
  .gni-money input { padding-left: 32px; }&#13;
&#13;
  /* helper + inline error text */&#13;
  .gni-note { margin-top: 6px; font-size: 12px; color: var(--gni-ink-54); }&#13;
  .gni-err  { margin-top: 6px; font-size: 12.5px; font-weight: 500; color: var(--gni-err); }&#13;
&#13;
  /* sub-heading inside a field grid (e.g. Address, Budget and timing) */&#13;
  .gni-sub {&#13;
    grid-column: 1 / -1;&#13;
    margin: 8px 0 -4px;&#13;
    font-size: 12px;&#13;
    font-weight: 700;&#13;
    letter-spacing: 0.06em;&#13;
    text-transform: uppercase;&#13;
    color: var(--gni-ink-54);&#13;
  }&#13;
&#13;
  /* "why we ask" explainer block */&#13;
  .gni-explain {&#13;
    grid-column: 1 / -1;&#13;
    padding: 14px 16px;&#13;
    border: 1px solid var(--gni-ink-10);&#13;
    border-radius: var(--gni-r-md);&#13;
    background: var(--gni-ink-04);&#13;
  }&#13;
  .gni-explain__title { margin: 0; font-size: 13px; font-weight: 700; color: var(--gni-ink); }&#13;
  .gni-explain__text  { margin: 4px 0 0; font-size: 13px; line-height: 1.55; color: var(--gni-ink-70); }&#13;
&#13;
  /* =======================================================&#13;
     DOCUMENTS · drop zone + file list&#13;
     ======================================================= */&#13;
  /* Native input is driven programmatically via c.pickFiles(); kept in&#13;
     the DOM but visually hidden. Not display:none, because some browsers&#13;
     won't fire a programmatic .click() on a display:none input. */&#13;
  .gni-file-input {&#13;
    position: absolute;&#13;
    width: 1px;&#13;
    height: 1px;&#13;
    margin: -1px;&#13;
    padding: 0;&#13;
    border: 0;&#13;
    overflow: hidden;&#13;
    clip: rect(0 0 0 0);&#13;
    opacity: 0;&#13;
  }&#13;
&#13;
  .gni-drop {&#13;
    padding: 32px 24px;&#13;
    text-align: center;&#13;
    border: 2px dashed var(--gni-line);&#13;
    border-radius: var(--gni-r-lg);&#13;
    background: var(--gni-ink-04);&#13;
    transition: border-color 0.15s, background 0.15s;&#13;
&#13;
    &amp;.is-over   { border-color: var(--gni-brand); background: var(--gni-brand-08); }&#13;
    &amp;.has-error { border-color: var(--gni-err-bd); }&#13;
  }&#13;
  .gni-drop__mark  { font-size: 32px; color: var(--gni-ink-40); }&#13;
  .gni-drop__title { margin: 12px 0 0; font-size: 16px; font-weight: 700; }&#13;
  .gni-drop__text  { margin: 4px 0 16px; font-size: 13px; color: var(--gni-ink-54); }&#13;
  .gni-drop__btn   { margin: 0 auto; }&#13;
&#13;
  /* a long list scrolls rather than pushing submit off-screen (patch #5) */&#13;
  .gni-files {&#13;
    margin: 16px 0 0;&#13;
    padding: 0;&#13;
    list-style: none;&#13;
    display: flex;&#13;
    flex-direction: column;&#13;
    gap: 8px;&#13;
    max-height: 320px;&#13;
    overflow-y: auto;&#13;
  }&#13;
  .gni-file {&#13;
    display: flex;&#13;
    align-items: center;&#13;
    gap: 12px;&#13;
    padding: 10px 12px;&#13;
    border: 1px solid var(--gni-line);&#13;
    border-radius: var(--gni-r-md);&#13;
    background: var(--gni-surface);&#13;
  }&#13;
  .gni-file__mark { flex: 0 0 auto; font-size: 16px; color: var(--gni-ink-54); }&#13;
  .gni-file__body { flex: 1; min-width: 0; display: flex; flex-direction: column; }&#13;
  .gni-file__name {&#13;
    font-size: 13.5px;&#13;
    font-weight: 600;&#13;
    white-space: nowrap;&#13;
    overflow: hidden;&#13;
    text-overflow: ellipsis;&#13;
  }&#13;
  .gni-file__size { font-size: 12px; color: var(--gni-ink-54); }&#13;
  .gni-file__x {&#13;
    flex: 0 0 auto;&#13;
    display: grid;&#13;
    place-items: center;&#13;
    width: 28px;&#13;
    height: 28px;&#13;
    border: none;&#13;
    border-radius: 50%;&#13;
    background: none;&#13;
    color: var(--gni-ink-54);&#13;
    cursor: pointer;&#13;
    transition: background 0.15s, color 0.15s;&#13;
&#13;
    &amp;:hover { background: var(--gni-err-bg); color: var(--gni-err); }&#13;
  }&#13;
&#13;
  /* =======================================================&#13;
     REVIEW · recap + confirm&#13;
     ======================================================= */&#13;
  .gni-review { display: flex; flex-direction: column; gap: 16px; }&#13;
&#13;
  .gni-recap {&#13;
    padding: 18px 20px;&#13;
    border: 1px solid var(--gni-line);&#13;
    border-radius: var(--gni-r-md);&#13;
  }&#13;
  .gni-recap__head {&#13;
    display: flex;&#13;
    align-items: center;&#13;
    justify-content: space-between;&#13;
    margin-bottom: 12px;&#13;
  }&#13;
  .gni-recap__title { margin: 0; font-size: 15px; font-weight: 700; }&#13;
  .gni-recap__edit {&#13;
    padding: 4px 10px;&#13;
    border: none;&#13;
    border-radius: var(--gni-r-sm);&#13;
    background: none;&#13;
    color: var(--gni-brand);&#13;
    font-size: 13px;&#13;
    font-weight: 600;&#13;
    cursor: pointer;&#13;
    transition: background 0.15s;&#13;
&#13;
    &amp;:hover { background: var(--gni-brand-08); }&#13;
  }&#13;
  .gni-recap__rows { margin: 0; display: flex; flex-direction: column; gap: 10px; }&#13;
  .gni-recap__row {&#13;
    display: grid;&#13;
    grid-template-columns: 180px 1fr;&#13;
    gap: 12px;&#13;
    align-items: baseline;&#13;
  }&#13;
  .gni-recap__row dt { margin: 0; font-size: 13px; font-weight: 600; color: var(--gni-ink-54); }&#13;
  .gni-recap__row dd { margin: 0; font-size: 13.5px; color: var(--gni-ink); word-break: break-word; }&#13;
  .gni-recap__row dd.is-blank,&#13;
  .gni-recap__files.is-blank { color: var(--gni-ink-40); font-style: italic; }&#13;
  .gni-recap__files { margin: 0; font-size: 13.5px; }&#13;
&#13;
  .gni-confirm {&#13;
    display: flex;&#13;
    align-items: flex-start;&#13;
    gap: 10px;&#13;
    margin: 0;&#13;
    padding: 16px 18px;&#13;
    border: 1px solid var(--gni-line);&#13;
    border-radius: var(--gni-r-md);&#13;
    background: var(--gni-ink-04);&#13;
    cursor: pointer;&#13;
&#13;
    input {&#13;
      flex: 0 0 auto;&#13;
      width: 16px;&#13;
      height: 16px;&#13;
      margin-top: 2px;&#13;
      accent-color: var(--gni-brand);&#13;
    }&#13;
    span { font-size: 13.5px; line-height: 1.5; color: var(--gni-ink-70); }&#13;
  }&#13;
&#13;
  /* =======================================================&#13;
     ACTIONS + upload progress&#13;
     ======================================================= */&#13;
  .gni-actions {&#13;
    display: flex;&#13;
    align-items: center;&#13;
    justify-content: space-between;&#13;
    gap: 12px;&#13;
    margin-top: 26px;&#13;
    padding-top: 22px;&#13;
    border-top: 1px solid var(--gni-line);&#13;
  }&#13;
  .gni-actions__right { display: flex; gap: 10px; }&#13;
&#13;
  .gni-progressbar {&#13;
    position: relative;&#13;
    height: 4px;&#13;
    margin-top: 16px;&#13;
    border-radius: 999px;&#13;
    background: var(--gni-ink-10);&#13;
    overflow: hidden;&#13;
&#13;
    span {&#13;
      position: absolute;&#13;
      top: 0;&#13;
      left: 0;&#13;
      height: 100%;&#13;
      border-radius: 999px;&#13;
      background: var(--gni-brand);&#13;
      transition: width 0.3s ease;&#13;
    }&#13;
  }&#13;
&#13;
  /* =======================================================&#13;
     BUTTONS&#13;
     ======================================================= */&#13;
  .gni-btn {&#13;
    display: inline-flex;&#13;
    align-items: center;&#13;
    justify-content: center;&#13;
    gap: 8px;&#13;
    padding: 10px 18px;&#13;
    font-size: 14px;&#13;
    font-weight: 600;&#13;
    line-height: 1.2;&#13;
    white-space: nowrap;&#13;
    text-decoration: none;&#13;
    border: 1px solid transparent;&#13;
    border-radius: var(--gni-r-sm);&#13;
    cursor: pointer;&#13;
    transition: background 0.15s, border-color 0.15s, color 0.15s, box-shadow 0.15s;&#13;
&#13;
    &amp;:focus-visible { outline: none; box-shadow: 0 0 0 3px var(--gni-brand-16); }&#13;
    &amp;:disabled { opacity: 0.5; cursor: not-allowed; }&#13;
  }&#13;
  .gni-btn--primary {&#13;
    background: var(--gni-brand);&#13;
    color: #fff;&#13;
    &amp;:hover:not(:disabled) { background: var(--gni-brand-hover); }&#13;
  }&#13;
  .gni-btn--ghost {&#13;
    background: var(--gni-surface);&#13;
    border-color: var(--gni-line);&#13;
    color: var(--gni-ink);&#13;
    &amp;:hover:not(:disabled) { background: var(--gni-ink-04); border-color: var(--gni-ink-40); }&#13;
  }&#13;
  .gni-btn--quiet {&#13;
    padding: 8px 12px;&#13;
    background: none;&#13;
    color: var(--gni-ink-54);&#13;
    &amp;:hover:not(:disabled) { background: var(--gni-ink-04); color: var(--gni-ink); }&#13;
  }&#13;
&#13;
  /* =======================================================&#13;
     RESPONSIVE&#13;
     ======================================================= */&#13;
  @media (max-width: 880px) {&#13;
    .gni-body { grid-template-columns: 1fr; }&#13;
&#13;
    .gni-rail { position: static; }&#13;
    .gni-rail__label { display: none; }&#13;
    .gni-steps {&#13;
      flex-direction: row;&#13;
      flex-wrap: nowrap;&#13;
      overflow-x: auto;&#13;
      gap: 6px;&#13;
      -webkit-overflow-scrolling: touch;&#13;
    }&#13;
    .gni-step { flex: 0 0 auto; padding: 8px 10px; }&#13;
    .gni-step__note { display: none; }&#13;
  }&#13;
&#13;
  @media (max-width: 760px) {&#13;
    .gni-field--third,&#13;
    .gni-field--quarter { grid-column: span 6; }&#13;
  }&#13;
&#13;
  @media (max-width: 640px) {&#13;
    .gni-panel { padding: 20px 18px; }&#13;
&#13;
    .gni-context { flex-direction: column; align-items: flex-start; }&#13;
&#13;
    .gni-field--half,&#13;
    .gni-field--third,&#13;
    .gni-field--quarter { grid-column: span 12; }&#13;
&#13;
    .gni-recap__row { grid-template-columns: 1fr; gap: 2px; }&#13;
&#13;
    .gni-actions { flex-direction: column-reverse; align-items: stretch; }&#13;
    .gni-actions__right { flex-direction: column; }&#13;
    .gni-btn { width: 100%; }&#13;
  }&#13;
&#13;
  /* respect reduced-motion */&#13;
  @media (prefers-reduced-motion: reduce) {&#13;
    * { transition: none !important; }&#13;
  }&#13;
}</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list/>
        <has_preview>false</has_preview>
        <id>applicant_form</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {
  
}]]></link>
        <name>Applicant_form</name>
        <option_schema/>
        <public>false</public>
        <roles>x_enno_gatenow.service_applicant_user</roles>
        <script><![CDATA[(function () {

  /* =========================================================
     GATENOW · APPLICANT INTAKE · Server script

     WHY THIS IS A FULL REPLACEMENT

     The version in use was marked READ ONLY and had no save
     actions in it at all. So the submit button posted a payload
     that nothing handled, which is why attachments never
     arrived: there was no code to write them. That is the same
     reason the draft save was doing nothing useful except
     forcing a widget re-render, which is the flicker.

     Three field name corrections while I was in here:

       grant_program  ->  program
           The applicant table field is program. Querying and
           prefilling grant_program silently matched nothing, so
           no draft was ever found.

       name           ->  program_name
           On the grant program table. Reading name returned
           empty, which is why the program dropdown rendered a
           list of blank options.

       submitted      ->  state
           There is no submitted field. Whether an application
           has gone in is held in state.

     The program is also read from either URL parameter, because
     the program detail page sends sys_id and the older links
     send program.
     ========================================================= */

  var APPLICANT = 'x_enno_gatenow_service_applicants';
  var PROGRAM   = 'x_enno_gatenow_grant_program';

  var currentUser = gs.getUserID();

  data.success          = false;
  data.error            = '';
  data.message          = '';
  data.alreadySubmitted = false;

  if (typeof input === 'undefined' || input === null) input = {};


  /* ---------- helpers ---------- */

  function param(key, dflt) {
    try {
      if (input && input[key] !== undefined && input[key] !== null) return input[key];
    } catch (e) { /* fall through */ }
    return dflt;
  }

  function cleanDate(v) {
    if (!v) return '';
    v = String(v);
    if (v.indexOf('T') > -1) return v.split('T')[0];
    var m = v.match(/\d{4}-\d{2}-\d{2}/);
    return m ? m[0] : v;
  }

  function programRef() {
    return String(param('grant_program', '') || param('program', '') || '');
  }


  /* ---------- which program ----------
     The detail page hands over sys_id. Older links use program.
     Either is accepted so a bookmarked URL still works. */
  var programParam = $sp.getParameter('program') || $sp.getParameter('sys_id') || '';
  data.program_param = programParam;

  var actionVal = String(param('action', ''));


  /* ---------- lookups ---------- */

  function alreadyIn(userId, programId) {
    if (!userId || !programId) return false;
    var gr = new GlideRecordSecure(APPLICANT);
    gr.addQuery('user', userId);
    gr.addQuery('program', programId);
    /* anything past draft counts as submitted */
    gr.addQuery('state', '!=', 'draft');
    gr.query();
    return gr.hasNext();
  }

  function findDraft(userId, programId) {
    if (!userId || !programId) return '';
    var gr = new GlideRecordSecure(APPLICANT);
    gr.addQuery('user', userId);
    gr.addQuery('program', programId);
    gr.addQuery('state', 'draft');
    gr.orderByDesc('sys_updated_on');
    gr.setLimit(1);
    gr.query();
    return gr.next() ? gr.getUniqueValue() : '';
  }

  /* category is never taken from the form, it is read off the
     program so the two can never disagree */
  function categoryOf(programId) {
    if (!programId) return '';
    var gr = new GlideRecordSecure(PROGRAM);
    return gr.get(programId) ? (gr.getValue('category') || '') : '';
  }


  /* ---------- write ---------- */

  function mapFields(rec) {

    var text = [
      'first_name', 'middle_name', 'last_name', 'email', 'phone_number',
      'street', 'city', 'zip_postal_code', 'country',
      'ssn', 'gender', 'race', 'ethnicity', 'education_level',
      'marital_status', 'military_status', 'disability_status',
      'citizenship_status', 'tribal_affiliation',
      'applicant_type', 'identity_type', 'uei', 'tin_ein', 'organization',
      'proposal_title', 'short_description', 'proposal_description',
      'project_goals_and_objectives', 'project_activities_methodology'
    ];

    for (var i = 0; i < text.length; i++) {
      rec.setValue(text[i], param(text[i], ''));
    }

    var bd = cleanDate(param('birthdate', ''));
    if (bd) rec.setValue('birthdate', bd);

    var sd = cleanDate(param('project_start_date', ''));
    if (sd) rec.setValue('project_start_date', sd);

    var ed = cleanDate(param('project_end_date', ''));
    if (ed) rec.setValue('project_end_date', ed);

    var budget = param('requested_budget', '');
    if (budget !== '' && budget !== null) rec.setValue('requested_budget', budget);

    var prog = programRef();
    if (prog) {
      rec.setValue('program', prog);
      rec.setValue('program_category', categoryOf(prog));
    }

    rec.setValue('user', currentUser);
    if (rec.isValidField('submitted_by')) rec.setValue('submitted_by', currentUser);
  }

  function attach(sysId, files) {
    if (!files || !files.length) return 0;

    var rec = new GlideRecordSecure(APPLICANT);
    if (!rec.get(sysId)) return 0;

    var done = 0;

    for (var i = 0; i < files.length; i++) {
      var f = files[i];
      if (!f || !f.data) continue;
      try {
        var id = new GlideSysAttachment().writeBase64(rec, f.name, f.type, f.data);
        if (id) done++;
        else gs.error('[Intake] attachment rejected: ' + f.name);
      } catch (e) {
        gs.error('[Intake] attachment failed on ' + f.name + ': ' + e.message);
      }
    }

    return done;
  }


  /* ============================================================
     ACTION · SUBMIT
     ============================================================ */
  if (actionVal === 'save_applicant') {
    try {

      var prog = programRef();

      if (alreadyIn(currentUser, prog)) {
        data.alreadySubmitted = true;
        data.error = gs.getMessage('You have already applied to this program.');
        return;
      }

      var gr = new GlideRecordSecure(APPLICANT);
      var target = String(param('applicant_sys_id', ''));
      var isUpdate = false;

      if (target && gr.get(target)) {
        isUpdate = true;
      } else {
        gr.initialize();
      }

      mapFields(gr);
      gr.setValue('state', 'new');

      if (!isUpdate) {
        gr.setValue('submission_date', new GlideDateTime().getValue());
      }

      var savedId = isUpdate ? gr.update() : gr.insert();

      if (!savedId) {
        data.error = gs.getMessage('The application could not be saved. Nothing was lost, try again.');
        return;
      }

      var files = param('attachments', []);
      var attached = attach(savedId, files);

      if (files && files.length && attached === 0) {
        /* the record is in, but the applicant needs to know the
           documents are not */
        data.error = gs.getMessage('Your application was saved but the documents did not attach. Open the application from your dashboard and add them.');
      }

      data.success          = true;
      data.applicant_sys_id = String(savedId);
      data.attachedCount    = attached;
      data.message          = gs.getMessage('Application submitted');

    } catch (e) {
      gs.error('[Intake] submit failed: ' + e.toString());
      data.error = gs.getMessage('Something went wrong submitting the application.') + ' ' + e.message;
    }
  }


  /* ============================================================
     PAGE LOAD
     ============================================================ */
  if (!actionVal) {

    if (programParam && alreadyIn(currentUser, programParam)) {
      data.alreadySubmitted = true;
      data.error = gs.getMessage('You have already applied to this program.');
    }

    if (programParam && !data.alreadySubmitted) {
      data.applicant_sys_id = findDraft(currentUser, programParam);
    }
  }


  /* ============================================================
     PREFILL
     ============================================================ */
  data.prefill = {};

  if (data.applicant_sys_id) {
    var p = new GlideRecordSecure(APPLICANT);
    if (p.get(data.applicant_sys_id)) {

      var read = [
        'first_name', 'middle_name', 'last_name', 'email', 'phone_number',
        'street', 'city', 'zip_postal_code', 'country', 'birthdate',
        'ssn', 'gender', 'race', 'ethnicity', 'education_level',
        'marital_status', 'military_status', 'disability_status',
        'citizenship_status', 'tribal_affiliation',
        'applicant_type', 'identity_type', 'uei', 'tin_ein', 'organization',
        'proposal_title', 'short_description', 'proposal_description',
        'project_goals_and_objectives', 'project_activities_methodology',
        'requested_budget', 'project_start_date', 'project_end_date',
        'program_category'
      ];

      for (var r = 0; r < read.length; r++) {
        data.prefill[read[r]] = p.getValue(read[r]) || '';
      }

      /* the form field is called grant_program, the column is
         program */
      data.prefill.grant_program = p.getValue('program') || programParam || '';
    }
  }

  if (!data.prefill.grant_program && programParam) {
    data.prefill.grant_program = programParam;
  }


  /* ============================================================
     PROGRAMS
     program_name, not name. Reading name returned empty, which
     is why the dropdown was a list of blanks.
     ============================================================ */
  data.programs = [];

  var pg = new GlideRecordSecure(PROGRAM);
  pg.addQuery('active', true);
  pg.orderBy('program_name');
  pg.query();

  while (pg.next()) {
    data.programs.push({
      sys_id  : pg.getUniqueValue(),
      name    : pg.getValue('program_name') || pg.getValue('number'),
      number  : pg.getValue('number') || '',
      category: pg.getValue('category') || ''
    });
  }


  /* ============================================================
     CHOICES
     Read from the dictionary first so the form can never drift
     from the table. The declared list is only a fallback for
     when sys_choice is not readable by this user, which is
     common for external accounts.

     ADJUST the fallback values if any of them differ from the
     dictionary, because a wrong value saves an empty field
     without complaining.
     ============================================================ */

  var FALLBACK = {
    applicant_type: [
      { value: 'Government',   label: 'Government' },
      { value: 'Individual',   label: 'Individual' },
      { value: 'Organization', label: 'Organization' }
    ],
    citizenship_status: [
      { value: 'Citizen',            label: 'Citizen' },
      { value: 'Non-Immigrant',      label: 'Non-Immigrant' },
      { value: 'Permanent Resident', label: 'Permanent Resident' },
      { value: 'Undocumented',       label: 'Undocumented' }
    ],
    disability_status: [
      { value: 'yes',         label: 'Yes, I have a disability' },
      { value: 'no',          label: 'No, I do not have a disability' },
      { value: 'do_not_wish', label: 'I do not wish to provide this information' }
    ],
    education_level: [
      { value: 'Associate degree',                     label: 'Associate degree' },
      { value: 'bachelor_degree',                      label: "Bachelor's degree" },
      { value: 'Doctorate',                            label: 'Doctorate' },
      { value: 'High school or equivalent',            label: 'High school or equivalent' },
      { value: 'Masters degree',                       label: "Master's degree" },
      { value: 'Professional',                         label: 'Professional' },
      { value: 'Some college coursework completed',    label: 'Some college coursework completed' },
      { value: 'Technical or occupational certificate', label: 'Technical or occupational certificate' },
      { value: 'Other',                                label: 'Other' }
    ],
    ethnicity: [
      { value: 'Hispanic or Latino',     label: 'Hispanic or Latino' },
      { value: 'Not Hispanic or Latino', label: 'Not Hispanic or Latino' },
      { value: 'I do not wish to provide this information', label: 'I do not wish to provide this information' }
    ],
    gender: [
      { value: 'Female', label: 'Female' },
      { value: 'Male',   label: 'Male' },
      { value: 'Other',  label: 'Other' }
    ],
    identity_type: [
      { value: "Driver's License Number",     label: "Driver's License Number" },
      { value: 'Medicare Number',             label: 'Medicare Number' },
      { value: 'Social Security Number',      label: 'Social Security Number' },
      { value: 'State Identification Number', label: 'State Identification Number' }
    ],
    marital_status: [
      { value: 'Single',   label: 'Single' },
      { value: 'Married',  label: 'Married' },
      { value: 'Divorced', label: 'Divorced' },
      { value: 'Widowed',  label: 'Widowed' }
    ],
    military_status: [
      { value: 'active_duty',       label: 'Active duty' },
      { value: 'veterans',          label: 'Veteran' },
      { value: 'no_military_duty',  label: 'No military duty' },
      { value: 'i_do_not_wish_to_provide_this_information', label: 'I do not wish to provide this information' }
    ],
    race: [
      { value: 'American Indian or Alaska Native',          label: 'American Indian or Alaska Native' },
      { value: 'Asian',                                     label: 'Asian' },
      { value: 'Black or African American',                 label: 'Black or African American' },
      { value: 'Native Hawaiian or Other pacific Islander',  label: 'Native Hawaiian or Other Pacific Islander' },
      { value: 'White',                                     label: 'White' },
      { value: 'I do not wish to provide this informantion', label: 'I do not wish to provide this information' }
    ],
    tribal_affiliation: [
      { value: 'Yes', label: 'Yes' },
      { value: 'No',  label: 'No' }
    ]
  };

  function choicesFor(element) {

    var out = [];

    try {
      var ch = new GlideRecordSecure('sys_choice');
      ch.addQuery('name', APPLICANT);
      ch.addQuery('element', element);

      /* language is often blank on scoped choices, and inactive
         is frequently null rather than false. Both were being
         filtered out before, which returned nothing. */
      var lang = ch.addQuery('language', 'en');
      lang.addOrCondition('language', '');
      lang.addOrCondition('language', 'NULL');

      var act = ch.addQuery('inactive', false);
      act.addOrCondition('inactive', '');

      ch.orderBy('sequence');
      ch.orderBy('label');
      ch.query();

      while (ch.next()) {
        var v = ch.getValue('value');
        if (v === null || v === undefined || v === '') continue;
        out.push({ value: v, label: ch.getValue('label') || v });
      }
    } catch (e) {
      gs.info('[Intake] sys_choice not readable for ' + element + ': ' + e.message);
    }

    if (!out.length) {
      out = FALLBACK[element] || [];
      gs.info('[Intake] using declared choices for ' + element);
    }

    return out;
  }

  data.choices = {
    applicant_type    : choicesFor('applicant_type'),
    identity_type     : choicesFor('identity_type'),
    gender            : choicesFor('gender'),
    race              : choicesFor('race'),
    ethnicity         : choicesFor('ethnicity'),
    education_level   : choicesFor('education_level'),
    marital_status    : choicesFor('marital_status'),
    military_status   : choicesFor('military_status'),
    disability_status : choicesFor('disability_status'),
    citizenship_status: choicesFor('citizenship_status'),
    tribal_affiliation: choicesFor('tribal_affiliation')
  };


  /* ============================================================
     COPY
     ============================================================ */
  data.dashboardUrl = '/applicant?id=applicant_dashboard';

  data.t = {
    applyingTo   : gs.getMessage('Applying to'),
    noProgram    : gs.getMessage('No program selected yet'),
    cancel       : gs.getMessage('Cancel'),
    steps        : gs.getMessage('Steps'),
    step         : gs.getMessage('Step'),
    of           : gs.getMessage('of'),
    complete     : gs.getMessage('complete'),
    allRequired  : gs.getMessage('Every field on this step is required'),
    resumed      : gs.getMessage('We picked up where you left off. Everything you had filled in is still here.'),
    errorsFound  : gs.getMessage('things still need attention'),
    choose       : gs.getMessage('Choose one'),
    charsLeft    : gs.getMessage('characters left'),
    notGiven     : gs.getMessage('Not given'),
    edit         : gs.getMessage('Edit'),

    s1  : gs.getMessage('About you'),
    s1n : gs.getMessage('Name and contact'),
    s2  : gs.getMessage('Background'),
    s2n : gs.getMessage('For funder reporting'),
    s3  : gs.getMessage('Organisation'),
    s3n : gs.getMessage('Program and identifiers'),
    s4  : gs.getMessage('Your proposal'),
    s4n : gs.getMessage('The part only you can write'),
    s5  : gs.getMessage('Documents'),
    s5n : gs.getMessage('Attach your evidence'),
    s6  : gs.getMessage('Review and submit'),
    s6n : gs.getMessage('Last look before it goes'),

    h1 : gs.getMessage('Who the review team should contact about this application.'),
    h2 : gs.getMessage('Funders report on who their programs reach. None of it affects how your application is scored.'),
    h3 : gs.getMessage('Which program you are applying to, and how your organisation is identified.'),
    h4 : gs.getMessage('The project itself. Take your time here, this is what gets scored.'),
    h5 : gs.getMessage('Supporting documents. PDF, Word or images, up to 10MB each.'),
    h6 : gs.getMessage('Check it over. Nothing is sent until you press submit.'),

    firstName    : gs.getMessage('First name'),
    middleName   : gs.getMessage('Middle name'),
    lastName     : gs.getMessage('Last name'),
    email        : gs.getMessage('Email'),
    phone        : gs.getMessage('Phone number'),
    birthdate    : gs.getMessage('Date of birth'),
    address      : gs.getMessage('Address'),
    street       : gs.getMessage('Street'),
    city         : gs.getMessage('City'),
    zip          : gs.getMessage('Zip or postal code'),
    country      : gs.getMessage('Country'),
    ssn          : gs.getMessage('Social security number'),
    ssnNote      : gs.getMessage('Required for individual applicants. Leave blank if you are applying as an organisation or a government body.'),
    citizenship  : gs.getMessage('Citizenship status'),
    gender       : gs.getMessage('Gender'),
    race         : gs.getMessage('Race'),
    ethnicity    : gs.getMessage('Ethnicity'),
    education    : gs.getMessage('Education level'),
    marital      : gs.getMessage('Marital status'),
    military     : gs.getMessage('Military status'),
    disability   : gs.getMessage('Disability status'),
    tribal       : gs.getMessage('Tribal affiliation'),
    whyAsk       : gs.getMessage('Why we ask'),
    whyAskText   : gs.getMessage('These answers are used for reporting on who the program reaches. Each list includes an option to withhold, and choosing it does not count against you.'),
    program      : gs.getMessage('Grant program'),
    category     : gs.getMessage('Category'),
    applicantType: gs.getMessage('Applicant type'),
    organisation : gs.getMessage('Organisation'),
    identityType : gs.getMessage('Identity type'),
    uei          : gs.getMessage('UEI'),
    tin          : gs.getMessage('TIN or EIN'),
    proposalTitle: gs.getMessage('Proposal title'),
    shortDesc    : gs.getMessage('One line summary'),
    proposalDesc : gs.getMessage('Proposal description'),
    goals        : gs.getMessage('Goals and objectives'),
    method       : gs.getMessage('Activities and methodology'),
    moneyTime    : gs.getMessage('Budget and timing'),
    budget       : gs.getMessage('Requested budget'),
    startDate    : gs.getMessage('Project start date'),
    endDate      : gs.getMessage('Project end date'),

    dropTitle    : gs.getMessage('Drop your documents here'),
    dropText     : gs.getMessage('PDF, Word or image files, up to 10MB each'),
    chooseFiles  : gs.getMessage('Choose files'),
    remove       : gs.getMessage('Remove'),
    documents    : gs.getMessage('Documents'),
    filesAttached: gs.getMessage('files attached'),
    noFiles      : gs.getMessage('Nothing attached yet'),

    confirm      : gs.getMessage('The information above is accurate to the best of my knowledge.'),
    back         : gs.getMessage('Back'),
    continue     : gs.getMessage('Continue'),
    submit       : gs.getMessage('Submit application'),
    submitting   : gs.getMessage('Submitting'),

    eFirst      : gs.getMessage('First name is required'),
    eMiddle     : gs.getMessage('Middle name is required'),
    eLast       : gs.getMessage('Last name is required'),
    eEmail      : gs.getMessage('Email is required'),
    eEmailBad   : gs.getMessage('That email address does not look right'),
    ePhone      : gs.getMessage('Phone number is required'),
    eDob        : gs.getMessage('Date of birth is required'),
    eStreet     : gs.getMessage('Street address is required'),
    eCity       : gs.getMessage('City is required'),
    eZip        : gs.getMessage('Zip or postal code is required'),
    eCountry    : gs.getMessage('Country is required'),
    eCitizen    : gs.getMessage('Citizenship status is required'),
    eSsn        : gs.getMessage('Social security number is required for individual applicants'),
    eIsRequired : gs.getMessage('is required'),
    eProgram    : gs.getMessage('Choose the program you are applying to'),
    eAType      : gs.getMessage('Applicant type is required'),
    eIType      : gs.getMessage('Identity type is required'),
    eOrg        : gs.getMessage('Organisation is required'),
    eUei        : gs.getMessage('UEI is required'),
    eTin        : gs.getMessage('TIN or EIN is required'),
    eTitle      : gs.getMessage('Proposal title is required'),
    eShort      : gs.getMessage('A one line summary is required'),
    eDesc       : gs.getMessage('Proposal description is required'),
    eGoals      : gs.getMessage('Goals and objectives are required'),
    eMethod     : gs.getMessage('Activities and methodology are required'),
    eBudget     : gs.getMessage('Requested budget must be more than zero'),
    eStart      : gs.getMessage('Project start date is required'),
    eEnd        : gs.getMessage('Project end date is required'),
    eOrder      : gs.getMessage('The end date has to come after the start date'),
    eDocs       : gs.getMessage('Attach at least one supporting document'),
    eConfirm    : gs.getMessage('Please confirm the information is accurate'),

    ok            : gs.getMessage('OK'),
    tooBigTitle   : gs.getMessage('File too large'),
    tooBigText    : gs.getMessage('is over the 10MB limit.'),
    badTypeTitle  : gs.getMessage('File type not supported'),
    badTypeText   : gs.getMessage('is not a PDF, Word document or image.'),
    fileErrTitle  : gs.getMessage('Could not read your files'),
    fileErrText   : gs.getMessage('Something went wrong preparing the attachments. Try again, or remove them and submit without.'),
    submitErrTitle: gs.getMessage('Could not submit'),
    submitErrText : gs.getMessage('The application was not submitted. Nothing has been lost, try again.'),
    connErrTitle  : gs.getMessage('No connection'),
    connErrText   : gs.getMessage('We could not reach the server. Check your connection and try again.'),
    cancelTitle   : gs.getMessage('Leave this application?'),
    cancelText    : gs.getMessage('Anything you have filled in will be lost.'),
    keepGoing     : gs.getMessage('Keep filling it in'),
    discard       : gs.getMessage('Leave'),

    submitted   : gs.getMessage('Application submitted'),
    already     : gs.getMessage('You have already applied to this program'),
    blockedTitle: gs.getMessage('Already applied'),
    blockedText : gs.getMessage('There is already an application from you against this program.'),
    toDashboard : gs.getMessage('Back to dashboard'),
    redirecting : gs.getMessage('Taking you to your dashboard in'),
    goNow       : gs.getMessage('Go now')
  };

})();]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>umar.farooq</sys_created_by>
        <sys_created_on>2026-02-17 10:39:56</sys_created_on>
        <sys_id>dc9210ba2f877650d2a0d68fcea4e339</sys_id>
        <sys_mod_count>80</sys_mod_count>
        <sys_name>Applicant_form</sys_name>
        <sys_package display_value="GateNow" source="x_enno_gatenow">eabeaf98eb033a10520dfbdffdc0cd51</sys_package>
        <sys_policy/>
        <sys_scope display_value="GateNow">eabeaf98eb033a10520dfbdffdc0cd51</sys_scope>
        <sys_update_name>sp_widget_dc9210ba2f877650d2a0d68fcea4e339</sys_update_name>
        <sys_updated_by>umar.farooq</sys_updated_by>
        <sys_updated_on>2026-08-27 08:00:26</sys_updated_on>
        <template><![CDATA[<!-- =========================================================
     GATENOW · APPLICANT INTAKE
     Service Portal widget · HTML template

     Document upload no longer depends on ng-file-upload, which is
     not part of the Service Portal AngularJS bundle. With the
     library absent, ngf-select / ngf-drop never bound, so the
     picker never opened and dropped files were ignored. Step 5
     now uses a native <input type="file">; the client controller
     wires the change / drag / drop events itself once the step
     renders, so it works in any portal.
     ========================================================= -->

<div class="gni" ng-class="{'is-blocked': c.formBlocked}">

  <!-- ═══════════════ BLOCKED ═══════════════ -->
  <div class="gni-block" ng-if="c.formBlocked && !c.successBanner">
    <span class="gni-block__mark" aria-hidden="true"><i class="fa fa-check"></i></span>
    <h1 class="gni-block__title">{{c.data.t.blockedTitle}}</h1>
    <p class="gni-block__text">{{c.data.error || c.data.t.blockedText}}</p>
    <a class="gni-btn gni-btn--primary" ng-href="{{c.data.dashboardUrl}}">{{c.data.t.toDashboard}}</a>
  </div>


  <!-- ═══════════════ SUCCESS ═══════════════ -->
  <div class="gni-done" ng-if="c.successBanner">

    <span class="gni-done__mark" aria-hidden="true"><i class="fa fa-check"></i></span>
    <h1 class="gni-done__title">{{c.successMessage}}</h1>

    <!-- shown only when the record saved but the files did not -->
    <p class="gni-done__warn" ng-if="c.successWarning">{{c.successWarning}}</p>

    <p class="gni-done__text">{{c.data.t.redirecting}} <strong>{{c.countdown}}</strong></p>
    <a class="gni-btn gni-btn--ghost" ng-href="{{c.data.dashboardUrl}}">{{c.data.t.goNow}}</a>

  </div>


  <!-- ═══════════════ FORM ═══════════════ -->
  <div class="gni-wrap" ng-if="!c.formBlocked && !c.successBanner">

    <header class="gni-context">

      <div class="gni-context__lead">
        <span class="gni-context__label">{{c.data.t.applyingTo}}</span>
        <p class="gni-context__name">{{c.programName() || c.data.t.noProgram}}</p>
        <p class="gni-context__meta" ng-if="c.programMeta()">{{c.programMeta()}}</p>
      </div>

      <div class="gni-context__right">
        <button type="button" class="gni-btn gni-btn--quiet" ng-click="c.cancel()">
          {{c.data.t.cancel}}
        </button>
      </div>

    </header>


    <div class="gni-body">

      <!-- ── step rail ── -->
      <nav class="gni-rail" aria-label="{{c.data.t.steps}}">

        <p class="gni-rail__label">{{c.data.t.steps}}</p>

        <ol class="gni-steps">
          <li ng-repeat="s in c.steps track by s.n">
            <button type="button"
                    class="gni-step"
                    ng-class="{'is-on': c.currentStep === s.n,
                               'is-done': c.isDone(s.n),
                               'is-ahead': s.n > c.currentStep && !c.isDone(s.n)}"
                    ng-click="c.goToStep(s.n)">
              <span class="gni-step__mark" aria-hidden="true">
                <i class="fa fa-check" ng-if="c.stepShowsCheck(s.n)"></i>
                <span ng-if="!c.stepShowsCheck(s.n)">{{s.n}}</span>
              </span>
              <span class="gni-step__body">
                <span class="gni-step__name">{{s.label}}</span>
                <span class="gni-step__note">{{s.note}}</span>
              </span>
            </button>
          </li>
        </ol>

        <div class="gni-rail__foot">
          <span class="gni-rail__track" aria-hidden="true">
            <span class="gni-rail__fill" ng-style="{width: c.progress() + '%'}"></span>
          </span>
          <span class="gni-rail__pct">{{c.progress()}}% {{c.data.t.complete}}</span>
        </div>

      </nav>


      <!-- ── panel ── -->
      <main class="gni-panel">

        <p class="gni-resumed" ng-if="c.draftResumed">
          <i class="fa fa-info-circle" aria-hidden="true"></i>
          {{c.data.t.resumed}}
        </p>

        <header class="gni-panel__head">
          <span class="gni-panel__count">{{c.data.t.step}} {{c.currentStep}} {{c.data.t.of}} {{c.totalSteps}}</span>
          <h2 class="gni-panel__title">{{c.stepTitle()}}</h2>
          <p class="gni-panel__hint">{{c.stepHint()}}</p>
          <p class="gni-panel__req" ng-if="c.currentStep < 5">{{c.data.t.allRequired}}</p>
        </header>

        <div class="gni-errors" ng-if="c.errorCount()">
          <p class="gni-errors__title">{{c.errorCount()}} {{c.data.t.errorsFound}}</p>
          <ul class="gni-errors__list">
            <li ng-repeat="(f, m) in c.errors">{{m}}</li>
          </ul>
        </div>


        <!-- ═══ 1 · PERSONAL ═══ -->
        <div class="gni-fields" ng-if="c.currentStep === 1">

          <div class="gni-field gni-field--third">
            <label for="gni_first">{{c.data.t.firstName}}</label>
            <input id="gni_first" type="text" ng-model="c.form.first_name"
                   ng-class="{'has-error': c.errors.first_name}">
            <span class="gni-err" ng-if="c.errors.first_name">{{c.errors.first_name}}</span>
          </div>

          <div class="gni-field gni-field--third">
            <label for="gni_middle">{{c.data.t.middleName}}</label>
            <input id="gni_middle" type="text" ng-model="c.form.middle_name"
                   ng-class="{'has-error': c.errors.middle_name}">
            <span class="gni-err" ng-if="c.errors.middle_name">{{c.errors.middle_name}}</span>
          </div>

          <div class="gni-field gni-field--third">
            <label for="gni_last">{{c.data.t.lastName}}</label>
            <input id="gni_last" type="text" ng-model="c.form.last_name"
                   ng-class="{'has-error': c.errors.last_name}">
            <span class="gni-err" ng-if="c.errors.last_name">{{c.errors.last_name}}</span>
          </div>

          <div class="gni-field gni-field--half">
            <label for="gni_email">{{c.data.t.email}}</label>
            <input id="gni_email" type="email" ng-model="c.form.email"
                   ng-class="{'has-error': c.errors.email}">
            <span class="gni-err" ng-if="c.errors.email">{{c.errors.email}}</span>
          </div>

          <div class="gni-field gni-field--quarter">
            <label for="gni_phone">{{c.data.t.phone}}</label>
            <input id="gni_phone" type="tel" ng-model="c.form.phone_number"
                   ng-class="{'has-error': c.errors.phone_number}">
            <span class="gni-err" ng-if="c.errors.phone_number">{{c.errors.phone_number}}</span>
          </div>

          <div class="gni-field gni-field--quarter">
            <label for="gni_dob">{{c.data.t.birthdate}}</label>
            <input id="gni_dob" type="date" ng-model="c.form.birthdate"
                   ng-class="{'has-error': c.errors.birthdate}">
            <span class="gni-err" ng-if="c.errors.birthdate">{{c.errors.birthdate}}</span>
          </div>

          <p class="gni-sub">{{c.data.t.address}}</p>

          <div class="gni-field gni-field--full">
            <label for="gni_street">{{c.data.t.street}}</label>
            <input id="gni_street" type="text" ng-model="c.form.street"
                   ng-class="{'has-error': c.errors.street}">
            <span class="gni-err" ng-if="c.errors.street">{{c.errors.street}}</span>
          </div>

          <div class="gni-field gni-field--third">
            <label for="gni_city">{{c.data.t.city}}</label>
            <input id="gni_city" type="text" ng-model="c.form.city"
                   ng-class="{'has-error': c.errors.city}">
            <span class="gni-err" ng-if="c.errors.city">{{c.errors.city}}</span>
          </div>

          <div class="gni-field gni-field--third">
            <label for="gni_zip">{{c.data.t.zip}}</label>
            <input id="gni_zip" type="text" ng-model="c.form.zip_postal_code"
                   ng-class="{'has-error': c.errors.zip_postal_code}">
            <span class="gni-err" ng-if="c.errors.zip_postal_code">{{c.errors.zip_postal_code}}</span>
          </div>

          <div class="gni-field gni-field--third">
            <label for="gni_country">{{c.data.t.country}}</label>
            <input id="gni_country" type="text" ng-model="c.form.country"
                   ng-class="{'has-error': c.errors.country}">
            <span class="gni-err" ng-if="c.errors.country">{{c.errors.country}}</span>
          </div>

        </div>


        <!-- ═══ 2 · BACKGROUND ═══ -->
        <div class="gni-fields" ng-if="c.currentStep === 2">

          <div class="gni-explain gni-field--full">
            <p class="gni-explain__title">{{c.data.t.whyAsk}}</p>
            <p class="gni-explain__text">{{c.data.t.whyAskText}}</p>
          </div>

          <div class="gni-field gni-field--half">
            <label for="gni_ssn">{{c.data.t.ssn}}</label>
            <input id="gni_ssn" type="text" ng-model="c.form.ssn" autocomplete="off"
                   ng-class="{'has-error': c.errors.ssn}">
            <span class="gni-note" ng-if="!c.errors.ssn">{{c.data.t.ssnNote}}</span>
            <span class="gni-err" ng-if="c.errors.ssn">{{c.errors.ssn}}</span>
          </div>

          <div class="gni-field gni-field--half">
            <label for="gni_citizen">{{c.data.t.citizenship}}</label>
            <div class="gni-pick">
              <select id="gni_citizen" ng-model="c.form.citizenship_status"
                      ng-class="{'has-error': c.errors.citizenship_status}">
                <option value="">{{c.data.t.choose}}</option>
                <option ng-repeat="o in c.data.choices.citizenship_status track by o.value"
                        value="{{o.value}}">{{o.label}}</option>
              </select>
              <i class="fa fa-angle-down" aria-hidden="true"></i>
            </div>
            <span class="gni-err" ng-if="c.errors.citizenship_status">{{c.errors.citizenship_status}}</span>
          </div>

          <div class="gni-field gni-field--half" ng-repeat="f in c.demoFields track by f.key">
            <label for="gni_{{f.key}}">{{f.label}}</label>
            <div class="gni-pick">
              <select id="gni_{{f.key}}" ng-model="c.form[f.key]"
                      ng-class="{'has-error': c.errors[f.key]}">
                <option value="">{{c.data.t.choose}}</option>
                <option ng-repeat="o in c.data.choices[f.key] track by o.value"
                        value="{{o.value}}">{{o.label}}</option>
              </select>
              <i class="fa fa-angle-down" aria-hidden="true"></i>
            </div>
            <span class="gni-err" ng-if="c.errors[f.key]">{{c.errors[f.key]}}</span>
          </div>

        </div>


        <!-- ═══ 3 · ORGANISATION ═══ -->
        <div class="gni-fields" ng-if="c.currentStep === 3">

          <div class="gni-field gni-field--full">
            <label for="gni_program">{{c.data.t.program}}</label>
            <div class="gni-pick">
              <select id="gni_program" ng-model="c.form.grant_program"
                      ng-class="{'has-error': c.errors.grant_program}">
                <option value="">{{c.data.t.choose}}</option>
                <option ng-repeat="p in c.data.programs track by p.sys_id"
                        value="{{p.sys_id}}">{{p.name}}{{p.number ? '  ·  ' + p.number : ''}}</option>
              </select>
              <i class="fa fa-angle-down" aria-hidden="true"></i>
            </div>
            <span class="gni-note" ng-if="c.form.program_category">
              {{c.data.t.category}}: <strong>{{c.form.program_category}}</strong>
            </span>
            <span class="gni-err" ng-if="c.errors.grant_program">{{c.errors.grant_program}}</span>
          </div>

          <div class="gni-field gni-field--half">
            <label for="gni_atype">{{c.data.t.applicantType}}</label>
            <div class="gni-pick">
              <select id="gni_atype" ng-model="c.form.applicant_type"
                      ng-class="{'has-error': c.errors.applicant_type}">
                <option value="">{{c.data.t.choose}}</option>
                <option ng-repeat="o in c.data.choices.applicant_type track by o.value"
                        value="{{o.value}}">{{o.label}}</option>
              </select>
              <i class="fa fa-angle-down" aria-hidden="true"></i>
            </div>
            <span class="gni-err" ng-if="c.errors.applicant_type">{{c.errors.applicant_type}}</span>
          </div>

          <div class="gni-field gni-field--half">
            <label for="gni_org">{{c.data.t.organisation}}</label>
            <input id="gni_org" type="text" ng-model="c.form.organization"
                   ng-class="{'has-error': c.errors.organization}">
            <span class="gni-err" ng-if="c.errors.organization">{{c.errors.organization}}</span>
          </div>

          <div class="gni-field gni-field--half">
            <label for="gni_itype">{{c.data.t.identityType}}</label>
            <div class="gni-pick">
              <select id="gni_itype" ng-model="c.form.identity_type"
                      ng-class="{'has-error': c.errors.identity_type}">
                <option value="">{{c.data.t.choose}}</option>
                <option ng-repeat="o in c.data.choices.identity_type track by o.value"
                        value="{{o.value}}">{{o.label}}</option>
              </select>
              <i class="fa fa-angle-down" aria-hidden="true"></i>
            </div>
            <span class="gni-err" ng-if="c.errors.identity_type">{{c.errors.identity_type}}</span>
          </div>

          <div class="gni-field gni-field--quarter">
            <label for="gni_uei">{{c.data.t.uei}}</label>
            <input id="gni_uei" type="text" ng-model="c.form.uei"
                   ng-class="{'has-error': c.errors.uei}">
            <span class="gni-err" ng-if="c.errors.uei">{{c.errors.uei}}</span>
          </div>

          <div class="gni-field gni-field--quarter">
            <label for="gni_tin">{{c.data.t.tin}}</label>
            <input id="gni_tin" type="text" ng-model="c.form.tin_ein"
                   ng-class="{'has-error': c.errors.tin_ein}">
            <span class="gni-err" ng-if="c.errors.tin_ein">{{c.errors.tin_ein}}</span>
          </div>

        </div>


        <!-- ═══ 4 · PROPOSAL ═══ -->
        <div class="gni-fields" ng-if="c.currentStep === 4">

          <div class="gni-field gni-field--full">
            <label for="gni_ptitle">{{c.data.t.proposalTitle}}</label>
            <input id="gni_ptitle" type="text" ng-model="c.form.proposal_title"
                   ng-class="{'has-error': c.errors.proposal_title}">
            <span class="gni-err" ng-if="c.errors.proposal_title">{{c.errors.proposal_title}}</span>
          </div>

          <div class="gni-field gni-field--full">
            <label for="gni_short">{{c.data.t.shortDesc}}</label>
            <input id="gni_short" type="text" maxlength="160"
                   ng-model="c.form.short_description"
                   ng-class="{'has-error': c.errors.short_description}">
            <span class="gni-note" ng-if="!c.errors.short_description">{{c.left(c.form.short_description, 160)}}</span>
            <span class="gni-err" ng-if="c.errors.short_description">{{c.errors.short_description}}</span>
          </div>

          <div class="gni-field gni-field--full">
            <label for="gni_desc">{{c.data.t.proposalDesc}}</label>
            <textarea id="gni_desc" rows="6" ng-model="c.form.proposal_description"
                      ng-class="{'has-error': c.errors.proposal_description}"></textarea>
            <span class="gni-err" ng-if="c.errors.proposal_description">{{c.errors.proposal_description}}</span>
          </div>

          <div class="gni-field gni-field--full">
            <label for="gni_goals">{{c.data.t.goals}}</label>
            <textarea id="gni_goals" rows="5" ng-model="c.form.project_goals_and_objectives"
                      ng-class="{'has-error': c.errors.project_goals_and_objectives}"></textarea>
            <span class="gni-err" ng-if="c.errors.project_goals_and_objectives">{{c.errors.project_goals_and_objectives}}</span>
          </div>

          <div class="gni-field gni-field--full">
            <label for="gni_method">{{c.data.t.method}}</label>
            <textarea id="gni_method" rows="5" ng-model="c.form.project_activities_methodology"
                      ng-class="{'has-error': c.errors.project_activities_methodology}"></textarea>
            <span class="gni-err" ng-if="c.errors.project_activities_methodology">{{c.errors.project_activities_methodology}}</span>
          </div>

          <p class="gni-sub">{{c.data.t.moneyTime}}</p>

          <div class="gni-field gni-field--third">
            <label for="gni_budget">{{c.data.t.budget}}</label>
            <div class="gni-money">
              <span class="gni-money__sign" aria-hidden="true">$</span>
              <input id="gni_budget" type="number" min="0" step="1" inputmode="decimal"
                     ng-model="c.form.requested_budget"
                     ng-class="{'has-error': c.errors.requested_budget}">
            </div>
            <span class="gni-err" ng-if="c.errors.requested_budget">{{c.errors.requested_budget}}</span>
          </div>

          <div class="gni-field gni-field--third">
            <label for="gni_start">{{c.data.t.startDate}}</label>
            <input id="gni_start" type="date" ng-model="c.form.project_start_date"
                   ng-class="{'has-error': c.errors.project_start_date}">
            <span class="gni-err" ng-if="c.errors.project_start_date">{{c.errors.project_start_date}}</span>
          </div>

          <div class="gni-field gni-field--third">
            <label for="gni_end">{{c.data.t.endDate}}</label>
            <input id="gni_end" type="date" ng-model="c.form.project_end_date"
                   ng-class="{'has-error': c.errors.project_end_date}">
            <span class="gni-err" ng-if="c.errors.project_end_date">{{c.errors.project_end_date}}</span>
          </div>

        </div>


        <!-- ═══ 5 · DOCUMENTS ═══ -->
        <div class="gni-fields" ng-if="c.currentStep === 5">

          <div class="gni-field--full">

            <!-- Native file input + dropzone. No ng-file-upload
                 dependency: the controller wires change / drag /
                 drop itself once this step renders. -->
            <div class="gni-drop"
                 ng-class="{'is-over': c.isDragging, 'has-error': c.errors.documents}">

              <span class="gni-drop__mark" aria-hidden="true"><i class="fa fa-cloud-upload"></i></span>
              <p class="gni-drop__title">{{c.data.t.dropTitle}}</p>
              <p class="gni-drop__text">{{c.data.t.dropText}}</p>

              <input type="file"
                     class="gni-file-input"
                     multiple
                     accept=".pdf,.doc,.docx,.jpg,.jpeg,.png"
                     style="position:absolute;width:1px;height:1px;opacity:0;overflow:hidden;clip:rect(0 0 0 0);margin:-1px;border:0;">

              <button type="button"
                      class="gni-btn gni-btn--ghost gni-drop__btn"
                      ng-click="c.pickFiles()">
                <i class="fa fa-paperclip" aria-hidden="true"></i>
                {{c.data.t.chooseFiles}}
              </button>

            </div>

            <span class="gni-err" ng-if="c.errors.documents">{{c.errors.documents}}</span>

            <ul class="gni-files" ng-if="c.uploadedFiles.length">
              <li class="gni-file" ng-repeat="f in c.uploadedFiles track by ($index + f.name)">
                <span class="gni-file__mark" aria-hidden="true"><i class="fa fa-file-o"></i></span>
                <span class="gni-file__body">
                  <span class="gni-file__name">{{f.name}}</span>
                  <span class="gni-file__size">{{c.formatFileSize(f.size)}}</span>
                </span>
                <button type="button" class="gni-file__x" ng-click="c.removeFile($index)"
                        aria-label="{{c.data.t.remove}}">
                  <i class="fa fa-times" aria-hidden="true"></i>
                </button>
              </li>
            </ul>

          </div>

        </div>


        <!-- ═══ 6 · REVIEW ═══ -->
        <div class="gni-review" ng-if="c.currentStep === 6">

          <div class="gni-recap" ng-repeat="g in c.recap() track by g.step">

            <header class="gni-recap__head">
              <h3 class="gni-recap__title">{{g.title}}</h3>
              <button type="button" class="gni-recap__edit" ng-click="c.goToStep(g.step)">
                {{c.data.t.edit}}
              </button>
            </header>

            <dl class="gni-recap__rows">
              <div class="gni-recap__row" ng-repeat="r in g.rows track by $index">
                <dt>{{r.label}}</dt>
                <dd ng-class="{'is-blank': !r.value}">{{r.value || c.data.t.notGiven}}</dd>
              </div>
            </dl>

          </div>

          <div class="gni-recap">
            <header class="gni-recap__head">
              <h3 class="gni-recap__title">{{c.data.t.documents}}</h3>
              <button type="button" class="gni-recap__edit" ng-click="c.goToStep(5)">{{c.data.t.edit}}</button>
            </header>
            <p class="gni-recap__files" ng-if="c.uploadedFiles.length">
              {{c.uploadedFiles.length}} {{c.data.t.filesAttached}}
            </p>
            <p class="gni-recap__files is-blank" ng-if="!c.uploadedFiles.length">{{c.data.t.noFiles}}</p>
          </div>

          <label class="gni-confirm">
            <input type="checkbox" ng-model="c.confirmed">
            <span>{{c.data.t.confirm}}</span>
          </label>
          <span class="gni-err" ng-if="c.errors.confirmed">{{c.errors.confirmed}}</span>

        </div>


        <!-- ── actions ── -->
        <footer class="gni-actions">

          <button type="button" class="gni-btn gni-btn--ghost"
                  ng-click="c.prevStep()" ng-disabled="c.currentStep === 1 || c.saving">
            {{c.data.t.back}}
          </button>

          <div class="gni-actions__right">

            <button type="button" class="gni-btn gni-btn--primary"
                    ng-if="c.currentStep < c.totalSteps"
                    ng-click="c.nextStep()" ng-disabled="c.saving">
              {{c.data.t.continue}}
              <i class="fa fa-arrow-right" aria-hidden="true"></i>
            </button>

            <button type="button" class="gni-btn gni-btn--primary"
                    ng-if="c.currentStep === c.totalSteps"
                    ng-click="c.submit()" ng-disabled="c.saving">
              <span ng-if="!c.saving">{{c.data.t.submit}}</span>
              <span ng-if="c.saving">{{c.data.t.submitting}}</span>
            </button>

          </div>

        </footer>

        <div class="gni-progressbar" ng-if="c.uploadProgress > 0">
          <span ng-style="{width: c.uploadProgress + '%'}"></span>
        </div>

      </main>

    </div>

  </div>

</div>]]></template>
    </sp_widget>
</record_update>
